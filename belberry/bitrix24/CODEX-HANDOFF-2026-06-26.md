# CODEX HANDOFF — cce delete-killswitch + sales_kpi ROP (26.06.2026)

> Снято 26.06 после разбора техдолга (Claude). Две задачи в зоне Codex (прод/SSH/категоризация).
> Все времена МСК. Секреты не печатать.

---

## Задача 1 — проверить прод-крон enrich/дедуп после внедрения killswitch

**Контекст.** 26.06 в `main` влиты защиты дедупа (PR #127/#128) — реакция на инцидент 20.06 (крон обогащения удалял рабочие ТМ-контакты). Теперь:

- **Глобальный killswitch** во ВСЕХ трёх копиях `BitrixClient` (crm_company_enrich, crm_deal_merge, empty_companies_score): любой `crm.{company,contact,deal,lead}.delete` возвращает `DELETE_BLOCKED` без `BITRIX_ALLOW_DELETE=1`. Unbind-методы (разрыв связи) НЕ блокируются.
- **Advisory-режим дедупа по умолчанию** (`CCE_CONTACT_DEDUPE_ADVISORY_ONLY=1`): дедуп только находит дубли и пишет в Sheets, ничего не сливает/удаляет.

**Что проверить на проде (Hetzner 188.34.206.115 / `ssh cloudbot-hz`):**

1. **Активен ли вообще enrich/дедуп-крон** (на 26.06 Claude не смог проверить — SSH заблокирован харнессом). Найти cron-записи enrich/`crm_company_enrich`, посмотреть, что запускается и с каким env.
2. **Не сломался ли молча какой-то легит-крон** из-за killswitch. Скоринг-крон `empty_companies_score` (2×/день 09:30+18:30) — НЕ удаляет, не затронут. Но проверить, нет ли крона, который реально звал `crm.*.delete`.
3. **Ручные delete-инструменты** — при запуске ставить `BITRIX_ALLOW_DELETE=1`, иначе вернут `DELETE_BLOCKED`:
   - `belberry/bitrix24/crm_deal_merge/scripts/delete_strict_empty_companies.py`
   - `belberry/bitrix24/empty_companies_score/scripts/delete_score3_safe.py`
   - (оба — ручной триаж пустышек, в кронах НЕ висят, так что молча ничего не сломалось.)

**Решение по дедупу:** если по дизайну дедуп должен оставаться advisory (только репорт) — ничего не делать, дефолт верный. Если нужен авто-merge — выставить `CCE_CONTACT_DEDUPE_ADVISORY_ONLY=0` (тогда работают тонкие guard'ы: не трогать контакты на сделках вне ТМ и с историей звонков).

---

## Задача 2 — решить, нужен ли sales-РОП Гордиенко в МОП-метрики (sales_kpi_dashboard)

**Контекст.** Старый PR #33 (закрыт как стейл, 528 behind) содержал уникальный, НЕ влитый в main фикс: в `sales_kpi_dashboard` MOP-regex не включает «РОП», а Гордиенко заведена в Bitrix с `WORK_POSITION="РОП"` (точная аббревиатура) → сейчас выпадает из МОП-блока.

**Это твоя активная зона** (модуль развивает Codex, regex переработан с мая) — поэтому Claude НЕ вливал: включать ли sales-РОП в МОП — решение о категоризации по текущему дизайну.

**Что на ветке-бэкапе `fix/sales-kpi-mop-include-rop` (цела):**
- `56c74e4` — добавить `^\s*роп\s*$` к MOP-regex (`config.py`) + тест `test_mop_regex_includes_sales_rop_but_not_other_rops` (РОП-аббревиатура матчится, «бывший РОП»/«админ-менеджер» — нет). **Конфликтует** с текущим `config.py` — нужен ручной мерж в актуальный regex.
- `2152dfb` — phase label через config+env (`alerts.py`/`config.py`), чистый, тоже не в main.

**Действие:** если по текущему дизайну Гордиенко должна быть в МОП-метриках — cherry-pick'нуть/переписать `56c74e4` в актуальный regex, прогнать тесты, PR. Иначе — оставить как есть (зафиксировать, что РОП намеренно отдельно).

---

## Что уже сделано Claude (для контекста, НЕ требует действий)

- Защиты дедупа собраны чисто на main (#127/#128/#129), старые PR #113/#114/#115/#116 закрыты как superseded.
- Enrich-фиксы из #28 салважены на main (#131): ветклиники→медицина, ИНН из Tilda + nbsp, реквизиты на /policy, перепроверка ликвидации. `partial enrichment` (4573205) НЕ салважен — глубоко конфликтует, частично в main; при нужде — отдельный заход.
- `feat/global-sales-dashboard` сведён с `main` (feat==main); решение вести SCC прямо в main (опция А).
