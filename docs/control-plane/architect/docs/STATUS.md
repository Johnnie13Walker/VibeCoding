# STATUS

## Формат записи

- Дата и время: `YYYY-MM-DD HH:MM MSK`
- Сценарий: nightly / health-check / manual
- Что сделано
- Какие проверки запущены
- Результат проверок
- Блокеры
- Следующий шаг

## Последний статус

- Дата и время: `2026-05-01 18:58 MSK`
- Сценарий: `manual netlify deploy`
- Что сделано: лендинг Acoola.Team размещён на бесплатном Netlify project `acoola-team-landing-codex`; добавлен `acoola-landing/netlify.toml` и `@netlify/plugin-nextjs`; создан отдельный Netlify site `4a825a02-c4f1-43d1-b02f-df0213a3cb77`; production URL: `https://acoola-team-landing-codex.netlify.app`. При первом deploy Netlify CLI использовал родительский link `architect/.netlify/state.json` и ошибочно выкатил Acoola в старый site `belberry-raci-report-20260408`; после обнаружения Acoola задеплоен явно через `--site 4a825a02-c4f1-43d1-b02f-df0213a3cb77`, а `belberry-raci-report-20260408` восстановлен на предыдущий production deploy `69dd006fb2560e8bbd1d0942`; внутри `acoola-landing/.netlify/state.json` зафиксирован правильный Acoola siteId, чтобы будущие deploy из этой папки не уходили в родительский project.
- Какие проверки запущены: `npx netlify status`, `npx netlify sites:create --name acoola-team-landing-codex --json`, `npx netlify deploy --build --prod --json --site 4a825a02-c4f1-43d1-b02f-df0213a3cb77`, `npx netlify api listSiteDeploys` для Acoola и Belberry, `npx netlify api restoreSiteDeploy` для Belberry, повторный `npx netlify status` из `acoola-landing`.
- Результат проверок: Acoola deploy `69f4cc8420e2be607d8b23aa` в состоянии `ready`, production alias `https://acoola-team-landing-codex.netlify.app`; Belberry site сейчас указывает на восстановленный deploy `69dd006fb2560e8bbd1d0942`; локальный Netlify status из `acoola-landing` теперь показывает `Current project: acoola-team-landing-codex`.
- Блокеры: публичную визуальную проверку через in-app browser выполнить не удалось из-за системного лимита Codex usage; состояние deploy подтверждено через Netlify API. Коммерческие факты Acoola.Team всё ещё требуют подтверждения перед реальной публикацией.
- Следующий шаг: открыть production URL вручную/после снятия лимита и визуально проверить страницу; затем подтвердить цены, статусы, отзывы и кейсовые метрики.

- Дата и время: `2026-05-01 18:43 MSK`
- Сценарий: `manual landing implementation`
- Что сделано: начато выполнение задачи из адаптированного Acoola prompt; создан отдельный Next.js-проект `acoola-landing/` с 10 секциями лендинга, типизированным контентом в `lib/content.ts`, компонентами UI, SEO-метаданными, Schema.org Organization, favicon и README; после audit high severity обновлены Next/React до актуальной ветки Next `16.2.4`, ESLint переведён на flat config, Framer Motion удалён из runtime ради Lighthouse Performance; `.gitignore` дополнен правилом `.next/`, чтобы build-артефакты не попадали в git.
- Какие проверки запущены: `npm install`, `npm run lint`, `npm run build`, `npm audit --omit=dev --audit-level=high`, production-запуск `npm run start -- -p 3002`, browser smoke-test через Playwright на `http://localhost:3002`, desktop/mobile viewport screenshots, проверка browser console, Lighthouse по категориям performance/accessibility/best-practices/seo.
- Результат проверок: `lint` зелёный, `build` зелёный, browser console без ошибок/предупреждений; Lighthouse: Performance `91`, Accessibility `96`, Best Practices `100`, SEO `100`; high severity audit findings закрыты, остаются только moderate findings внутри bundled `postcss` Next, для которых npm предлагает некорректный breaking downgrade.
- Блокеры: коммерческие факты Acoola.Team не подтверждены источниками: цены, год основания, NPS 100, статусы, отзывы и кейсовые метрики требуют проверки перед публикацией.
- Следующий шаг: подтвердить факты по официальным источникам Acoola.Team, заменить TODO-формулировки и при необходимости подключить реальную отправку формы/метрику.

- Дата и время: `2026-05-01 18:19 MSK`
- Сценарий: `manual prompt adaptation`
- Что сделано: прочитан исходный `/Users/pro2kuror/Downloads/acoola_claude_code_prompt.md` и создан Codex-адаптированный prompt для разработки лендинга Acoola.Team в `docs/prompts/acoola-codex-landing.md`; в новой версии убраны инструкции под Claude Code, добавлены рабочий цикл Codex, требования к проверкам, безопасная работа с неподтверждёнными фактами и критерии готовности.
- Какие проверки запущены: визуальная сверка исходного prompt, проверка структуры `docs/prompts`, `git status --short`, контрольный diff нового файла.
- Результат проверок: новый prompt добавлен как отдельный документ и не перезаписывает существующие prompt-файлы; неподтверждённые цены, даты и отзывы помечены как `TODO: подтвердить`.
- Блокеры: фактические цены, год основания, статусы, отзывы и часть кейсов требуют подтверждения по источникам Acoola.Team перед публикацией.
- Следующий шаг: при запуске задачи разработки лендинга использовать `docs/prompts/acoola-codex-landing.md` как входной prompt и отдельно подтвердить коммерческие факты.

- Дата и время: `2026-04-27 11:51 MSK`
- Сценарий: `manual scheduler repair`
- Что сделано: устранён повторный пропуск marketing dashboard. Причина подтверждена: блок `MARKETING_DASHBOARD_DAILY` в пользовательском `crontab` был закомментирован после TCC-проблемы Desktop/LaunchAgent. Скрипты сделаны переносимыми через `MARKETING_DASHBOARD_ROOT_DIR`, `MARKETING_DASHBOARD_ENGINEER_ENV` и `MARKETING_GOOGLE_SERVICE_ACCOUNT_JSON`. Установлен активный cron на runtime вне Desktop: `/Users/pro2kuror/.local/share/cloudbot/marketing-dashboard/architect`. Google service account перенесён в secret-file вне git: `/Users/pro2kuror/.config/openclo/assistant/secrets/finance-director-sheets-903611b799c3.json` с правами `600`.
- Какие проверки запущены: `bash -n scripts/run_marketing_dashboard_daily.sh`, `bash -n scripts/install_marketing_dashboard_cron.sh`, `python3 -m py_compile scripts/bitrix_field_audit_gd324.py scripts/refresh_marketing_dashboard_live.py scripts/send_marketing_dashboard_telegram_status.py`, `node --check scripts/build_cohort_filter_sheet.mjs`, `node --check scripts/verify_marketing_dashboard_live.mjs`, установка cron через `scripts/install_marketing_dashboard_cron.sh`, ручной runtime-запуск `/Users/pro2kuror/.local/share/cloudbot/marketing-dashboard/architect/scripts/run_marketing_dashboard_daily.sh`, фактическая отправка Telegram-отбивки.
- Результат проверок: runtime-запуск завершился `status=OK`, период `2026-01-01..2026-04-27`, `441` проверка, `0` ошибок; Google Sheets обновлён в `2026-04-27 11:46 MSK`; Telegram-отбивка завершилась `telegram_status=sent`. В `crontab` активны строки `08:00 МСК` для обновления и `08:15 МСК` для Telegram-статуса из runtime вне Desktop.
- Блокеры: нет по текущему ручному восстановлению. Остаётся наблюдать первый автоматический слот после переноса runtime.
- Следующий шаг: `2026-04-28` после `08:20 МСК` проверить `/Users/pro2kuror/.local/share/cloudbot/marketing-dashboard/architect/tmp/marketing_dashboard_daily.cron.log`, `/Users/pro2kuror/.local/share/cloudbot/marketing-dashboard/architect/tmp/marketing_dashboard_telegram.cron.log` и свежесть `marketing_dashboard_daily_status.json`.

- Дата и время: `2026-04-24 10:08 MSK`
- Сценарий: `manual incident repair`
- Что сделано: восстановлено пропущенное обновление маркетингового дашборда за `2026-04-24`. Причина инцидента: active scheduler отсутствовал после отключения локального macOS cron `2026-04-23`; первая ручная попытка упала на `crm.deal.list` из-за timeout Bitrix24. Добавлен безопасный override `BITRIX_TIMEOUT_SEC` в helper авторизации Bitrix и дефолт `90` секунд в daily runner, после чего дашборд успешно обновлён и Telegram-отбивка Ларисы отправлена.
- Какие проверки запущены: ручной запуск `/bin/bash scripts/run_marketing_dashboard_daily.sh`, повторный запуск с `BITRIX_TIMEOUT_SEC=90`, `node scripts/verify_marketing_dashboard_live.mjs`, `python3 scripts/send_marketing_dashboard_telegram_status.py --dry-run`, фактический `python3 scripts/send_marketing_dashboard_telegram_status.py`, `python3 -m py_compile scripts/bitrix_field_audit_gd324.py scripts/refresh_marketing_dashboard_live.py scripts/send_marketing_dashboard_telegram_status.py`.
- Результат проверок: status `OK`, период `2026-01-01..2026-04-24`, `431` проверка, `0` ошибок; Google Sheets обновлён в `2026-04-24 10:03 MSK`, verification OK; Telegram-отбивка завершилась `telegram_status=sent`.
- Блокеры: постоянный scheduler для marketing dashboard всё ещё не включён: локальный macOS cron/LaunchAgent ранее признан ненадёжным из-за TCC/Desktop, а server runtime пока не содержит этих скриптов. Нужен отдельный перенос scheduler на сервер или в другой надёжный runtime.
- Следующий шаг: вынести marketing dashboard runner и Telegram status sender в server-managed runtime/scheduler, затем включить `08:00/08:15 МСК` и проверить следующий утренний цикл.

- Дата и время: `2026-04-23 09:25 MSK`
- Сценарий: `manual morning schedule repair`
- Что сделано: проверен утренний цикл после перехода на `Cloudbot/...`. Локальный macOS cron не выполнил `08:00` и `08:15`; дашборд обновлён компенсирующим ручным запуском в `08:07-08:10 МСК`, Telegram-статус отправлен вручную. Попытка заменить локальный cron на LaunchAgents показала TCC-блокировку доступа фоновых задач к `~/Desktop/Cloudbot`, поэтому LaunchAgents выгружены и удалены, а локальные cron-дубли оставлены отключёнными до отдельного переноса runtime вне Desktop. Лариса не пришла к `09:00`, потому что серверный Debian cron игнорировал `CRON_TZ=Europe/Moscow` в `/etc/cron.d` и запускал `0 8 * * *` как `11:00 МСК`; Лариса отправлена вручную в `09:10 МСК`, затем серверный cron исправлен на явное UTC-выражение `0 5 * * *` (`08:00 МСК`)
- Какие проверки запущены: `pmset -g log`, `launchctl print`, проверка launchd stderr/stdout, ручной запуск `run_marketing_dashboard_daily.sh`, фактическая отправка `send_marketing_dashboard_telegram_status.py`, ручной запуск `./infra/orchestrator/run_workflow.sh larisa_daily_brief`, remote inspect `/etc/cron.d/cloudbot-larisa-daily-brief`, remote cron/syslog grep, `bash -n infra/orchestrator/workflows/larisa_agent_deploy.sh infra/orchestrator/workflows/cloudbot_runtime_verify.sh`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, `python3 -m unittest tests.test_larisa_agent tests.test_larisa_search tests.test_search_provider tests.test_system_health -q`, `bash checks/post_change_verify.sh`
- Результат проверок: маркетинговый дашборд за `2026-04-23` обновлён с `status=OK`, `418` проверок и `0` ошибок; Telegram-статус отправлен; Лариса отправлена в Telegram, отчёт `larisa_daily_brief_20260423_091049_MSK.txt`, `delivered=true`; серверный cron Ларисы теперь содержит `0 5 * * * root /usr/local/bin/cloudbot-larisa-daily-brief.sh`; `cloudbot_runtime_verify` подтверждает явное UTC-выражение для `08:00 МСК`; `52` Python tests OK; `post_change_verify` OK
- Блокеры: локальные задачи, которые живут в `~/Desktop/Cloudbot`, нельзя надёжно запускать через macOS LaunchAgent без TCC/Full Disk Access или переноса runtime вне Desktop. До отдельного controlled move локальные marketing dashboard задачи требуют ручного/операторского запуска или отдельного механизма вне macOS cron/LaunchAgent. Серверная Лариса исправлена и должна сработать завтра в `2026-04-24 08:00 МСК`
- Следующий шаг: завтра проверить серверный запуск Ларисы после `08:05 МСК`; отдельно решить архитектурно, переносить локальный `Cloudbot` runtime из Desktop в неприкрытый TCC каталог или переносить marketing dashboard schedule на сервер/Codex automation

- Дата и время: `2026-04-22 11:16 MSK`
- Сценарий: `manual post-migration testing`
- Что сделано: выполнена ручная имитация локальных cron-команд после перевода на `Cloudbot/...`; найден и исправлен дефект ежедневного маркетингового дашборда: скрипт запускался cron через `/bin/zsh`, хотя написан как bash-скрипт, а функция `write_status` использовала имя локальной переменной `status`, конфликтующее с read-only переменной zsh. Переменная переименована в `run_status`, install-скрипт и реальный `crontab` переведены на запуск через `/bin/bash`
- Какие проверки запущены: ручной запуск `/bin/bash /Users/pro2kuror/Desktop/Cloudbot/architect/scripts/run_marketing_dashboard_daily.sh` с cron-логированием, dry-run `send_marketing_dashboard_telegram_status.py`, dry-run `larisa_daily_brief` через `TELEGRAM_DRY_RUN=1 LARISA_TELEGRAM_DRY_RUN=1`, `bash -n` и `zsh -n` для marketing cron scripts, `/Users/pro2kuror/Desktop/Cloudbot/bin/verify_workspace.sh`, `bash scripts/verify_integrations.sh`, `bash checks/post_change_verify.sh`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, `python3 -m unittest tests.test_larisa_agent tests.test_larisa_search tests.test_search_provider tests.test_system_health -q`, `npm test` в `bot`, контроль `crontab -l`
- Результат проверок: повторный daily dashboard run завершился `status=OK`, live verification `406` проверок и `0` ошибок; Telegram status dry-run собрал корректное сообщение по свежему status-файлу; Larisa daily brief dry-run успешно сформировал отчёт; `crontab` теперь использует `/bin/bash` и пути `Cloudbot/...`; `verify_workspace`, `verify_integrations`, `post_change_verify`, `cloudbot_runtime_verify`, `52` Python unit tests и `npm test` завершились успешно
- Блокеры: фактический scheduled cron через новые строки будет подтверждён только на ближайших слотах `2026-04-23 08:00`, `08:15` и `09:00 MSK`; в текущей сессии выполнена эквивалентная ручная имитация команд. В логах Ларисы report path иногда отображается как physical `/Users/pro2kuror/Desktop/OpenClo/projects/engineer` из-за раскрытия symlink оболочкой, но запуск идёт из `Cloudbot/engineer` и runtime-проверки зелёные
- Следующий шаг: после ближайшего утреннего cron-цикла проверить свежие timestamps в `tmp/marketing_dashboard_daily.cron.log`, `tmp/marketing_dashboard_telegram.cron.log` и `reports/larisa_daily_brief.log`; затем перейти к отдельному документальному проходу по `docs/architecture/*`

- Дата и время: `2026-04-22 10:52 MSK`
- Сценарий: `manual controlled migration`
- Что сделано: выполнен первый безопасный этап controlled migration на единую точку входа `Cloudbot`: активные absolute-path ссылки в control-plane и инженерном контуре переведены на `/Users/pro2kuror/Desktop/Cloudbot/architect` и `/Users/pro2kuror/Desktop/Cloudbot/engineer`; пользовательский `crontab` также переведён на `Cloudbot/...`; физический перенос папок не выполнялся, текущая схема остаётся обратимой через symlink-и
- Какие проверки запущены: `/Users/pro2kuror/Desktop/Cloudbot/bin/verify_workspace.sh`, `crontab -l`, `bash -n` для shell-скриптов, `python3 -m py_compile` с `PYTHONPYCACHEPREFIX=/tmp`, `bash scripts/verify_integrations.sh`, `bash checks/post_change_verify.sh`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, `python3 -m unittest tests.test_larisa_agent tests.test_larisa_search tests.test_search_provider tests.test_system_health -q`, `npm test` в `bot`, dry-run `python3 scripts/send_marketing_dashboard_telegram_status.py --dry-run`, поиск старых путей в активных файлах
- Результат проверок: symlink-workspace валиден; `crontab` использует только `Cloudbot/...` для локальных задач; shell- и Python-синтаксис зелёные; `verify_integrations` завершился `Итог: ОК`; `post_change_verify` ОК; `cloudbot_runtime_verify` ОК; `52` Python unit tests ОК; `npm test` ОК; dry-run Telegram-статуса маркетингового дашборда корректно собрал сообщение с данными и ссылкой на дашборд
- Блокеры: в `docs/architecture/*` ещё остаются исторические ссылки на старые пути `/Users/pro2kuror/Desktop/architect` и `/Users/pro2kuror/Desktop/OpenClo/projects/engineer`; они не участвуют в runtime-запуске и требуют отдельного документального прохода, чтобы не смешивать operational migration с переписыванием старых аудитов. Реальный physical move пока специально не выполнялся
- Следующий шаг: наблюдать ближайшие локальные cron-слоты через `Cloudbot/...`; затем отдельным шагом обновить архитектурные документы и только после этого планировать физический перенос с обратными symlink-и со старых путей

- Дата и время: `2026-04-22 09:40 MSK`
- Сценарий: `manual`
- Что сделано: закрыта проблема отправки статуса маркетингового дашборда через Ларису Ивановну: токен Ларисы вынесен в локальный secret-file вне git `/Users/pro2kuror/.config/openclo/assistant/secrets/larisa_telegram.bot_token`, в пользовательский `.env.integrations` добавлен `LARISA_TELEGRAM_BOT_TOKEN_FILE`, а `scripts/send_marketing_dashboard_telegram_status.py` получил default fallback на этот secret-file, чтобы соседние ветки не требовали ручного добавления токена в окружение
- Какие проверки запущены: `python3 -m py_compile scripts/send_marketing_dashboard_telegram_status.py`, dry-run сборка сообщения, ручная проверка resolver-а `token_resolved=yes/chat_resolved=yes`, фактический запуск `python3 scripts/send_marketing_dashboard_telegram_status.py`
- Результат проверок: dry-run сообщение собрано корректно; фактическая отправка завершилась `telegram_status=sent`; права secret-file `600`, токен не добавлен в git
- Блокеры: отсутствуют
- Следующий шаг: при переносе/слиянии соседней ветки сохранить fallback в `send_marketing_dashboard_telegram_status.py` и не коммитить локальный secret-file

- Дата и время: `2026-04-22 09:22 MSK`
- Сценарий: `manual workspace merge audit`
- Что сделано: выполнена общая проверка после объединения рабочих папок в `Cloudbot`; подтверждено, что `Cloudbot` является symlink-точкой входа, а канонические пути `/Users/pro2kuror/Desktop/architect` и `/Users/pro2kuror/Desktop/OpenClo/projects/engineer` сохранены, поэтому старые cron/deploy-команды не сломаны самим переносом. Исправлены найденные последствия: отключён устаревший локальный cron `news_digest`, для которого отсутствует workflow; обновлён `owner_operating_contract_MSK.md`; исправлен `scripts/verify_integrations.sh`, чтобы env-переменные из `.env.integrations` экспортировались в подпроцессы проверок
- Какие проверки запущены: `./bin/verify_workspace.sh`, локальный `crontab -l`, поиск абсолютных путей, `./checks/check_access.sh`, `bash checks/instruction_conflicts.sh`, `bash checks/context_contract_verify.sh`, `bash scripts/verify_integrations.sh`, `bash checks/post_change_verify.sh`, `npm test` в `bot`, `python3 -m unittest tests.test_larisa_agent tests.test_larisa_search tests.test_search_provider tests.test_system_health -q`, remote `docker ps`, `curl /healthz`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, просмотр cron и логов на `ams-1-vm-76ds`
- Результат проверок: workspace symlink-и целые, broken symlink не найден; SSH-доступ ОК; OpenClaw gateway `running/healthy`; `cloudbot_runtime_verify` ОК; `post_change_verify` ОК; `verify_integrations` ОК; WAZZUP channels API возвращает 200; bot npm tests OK; 52 Python unit tests OK; Telegram provider Ларисы и коммерческого директора стартуют, в логах есть успешные `sendMessage`
- Блокеры: активных runtime-блокеров после проверки не осталось. Наблюдение: локальные `/health`, `/tasks`, `/search` без server env показывают ограничения, потому что секреты и live-state живут на сервере; это ожидаемая граница локального контура, а не поломка. WHOOP отправляет отчёт, но свежие recovery/sleep/cycle данные за текущий день пока частично отсутствуют у провайдера; это data-lag WHOOP, не эффект объединения папок
- Следующий шаг: оставить `Cloudbot` как навигационную папку-symlink до отдельной миграции абсолютных путей; не переносить физически `architect` и `engineer`, пока cron, docs и deploy-контракты не переведены на переменные окружения или вычисление root

- Дата и время: `2026-04-22 09:16 MSK`
- Сценарий: `manual incident repair`
- Что сделано: восстановлена Лариса Ивановна после отказа Telegram/OpenClaw-контура; найден отсутствующий локальный `infra/remote-ops.env` после объединения папок, восстановлен безопасный SSH-контур через `cloudbot-ssh-proxy`; на сервере `ams-1-vm-76ds` исправлены права `/root/.openclaw/openclaw.json` с `root:root` на `1000:1000`, из-за которых контейнер `openclaw-openclaw-gateway-1` уходил в restart loop; gateway перезапущен и стал `healthy`; серверный cron Ларисы приведён к `CRON_TZ=Europe/Moscow` и расписанию `08:00 МСК`; deploy-шаблон Ларисы теперь берёт MSK-расписание из `configs/schedule_contract.env`
- Какие проверки запущены: `./checks/check_access.sh`, `docker ps`, `curl http://127.0.0.1:18789/healthz`, `docker logs`, `docker exec ... npm run reminders:tick`, live `/usr/local/bin/cloudbot-larisa-daily-brief.sh`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, `python3 -m unittest tests.test_larisa_agent tests.test_larisa_search tests.test_search_provider`, `npm test` в `bot`
- Результат проверок: SSH-доступ ОК; `openclaw-openclaw-gateway-1` работает и `healthy`; health endpoint возвращает `{"ok":true,"status":"live"}`; Telegram provider `@larisa_ivanovna64_bot` стартовал; live-бриф Ларисы доставлен в Telegram; reminders tick завершился `reminders_ok`; `cloudbot_runtime_verify` завершён с `Итог: ОК`; targeted Python tests `38 OK`; bot npm tests OK
- Блокеры: общий `python3 checks/smoke_test.py` падает на устаревшем ожидании, что `/search` не должен быть в Telegram alias layer, хотя текущий контур Ларисы уже использует `/search`; это не активный runtime-инцидент, но smoke-check нужно отдельно синхронизировать с текущим user surface
- Следующий шаг: наблюдать следующие cron-прогоны после `2026-04-22 09:16 МСК`; отдельно обновить общий smoke-test под текущий контракт `/search`, чтобы post-change проверка снова была полностью зелёной

- Дата и время: `2026-04-18 07:23 MSK`
- Сценарий: `manual`
- Что сделано: доведено до боевого результата отключение навязчивого вечернего отчёта из legacy `todo-integration`. На live-хосте `ams-1-vm-76ds` в `/etc/cron.d/openclaw-todo-digest` строка `digest:evening` (`0 16 * * *`, то есть `19:00 MSK`) переведена в commented-состояние; это exact source карточки `🌙 Вечерний чек` со скрина пользователя. Дополнительно создан backup текущего отключённого cron-файла: `/var/backups/openclaw-todo-evening-disabled-state-20260418_072226_MSK`. Параллельно устранён дефект самого repo-managed apply-flow: [todo_digest_schedule.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/todo_digest_schedule.sh:1) теперь читает и переписывает root-owned cron через `sudo`, поэтому последующие отключения `digest:*` больше не должны падать на `Permission denied`.
- Какие проверки запущены: SSH-проверка `ops/ssh_happ.sh` на `72.56.83.251`, read-only просмотр `/etc/cron.d/openclaw-todo-digest` до правки, live apply безопасного отключения, `tail -n 20 /var/log/openclaw-todo-evening.log`, `bash -n infra/orchestrator/workflows/todo_digest_schedule.sh`, повторный `./infra/orchestrator/run_workflow.sh todo_digest_schedule inspect`
- Результат проверок: на бою legacy `digest:evening` больше не активен; повторный inspect через штатный workflow показывает `before.evening=commented`, а read-only cron-чтение фиксирует закомментированную строку `# 0 16 * * * ... npm run digest:evening`. Последний фактический send в логе относится к `2026-04-17`, новых отправок после отключения не зафиксировано. Shell-синтаксис обновлённого workflow корректен.
- Блокеры: отсутствуют по текущему инциденту. Исторический pre-change backup exact active-state не сохранился из-за найденного дефекта старого apply-flow, но текущее отключённое состояние сохранено в `/var/backups/...072226_MSK`, а исходная active-строка полностью видна в самом cron-комментарии и в control-plane статусе.
- Следующий шаг: наблюдать ближайший слот `19:00 MSK`; если сообщение не придёт, инцидент считать закрытым окончательно

- Дата и время: `2026-04-18 09:45 MSK`
- Сценарий: `manual`
- Что сделано: устранён источник ложного `Не настроено` в утреннем health-summary по уже заведённым интеграциям. В инженерном контуре [cloudbot/devops/system_health.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/cloudbot/devops/system_health.py:1) health-check теперь умеет учитывать server-side runtime для `WHOOP`, `Bitrix OAuth`, `Todoist`, `WAZZUP_WEBHOOK_FORWARD`, `WEBHOOK` и `Google Calendar OAuth` через read-only remote snapshot, а не только локальный env текущего workspace. Дополнительно обновлены тесты [tests/test_system_health.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/tests/test_system_health.py:1) и инженерная сводка интеграций [docs/api_integrations.md](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/docs/api_integrations.md:1). В control-plane расширен канонический реестр [providers/README.md](/Users/pro2kuror/Desktop/architect/providers/README.md:1): теперь там явно зафиксировано, что server-only контуры тоже считаются подключёнными провайдерами и не должны автоматически падать в `Не настроено`. Отдельно задокументирован auxiliary/server-side контур `Google Calendar OAuth`, который раньше всплывал в operational report без явного места в registry.
- Какие проверки запущены: `python3 -m unittest tests.test_system_health -q`, `python3 -m py_compile cloudbot/devops/system_health.py`, точечные ручные вызовы `_build_integrations_snapshot()`, `_check_bitrix_oauth()`, `_check_todoist_api()` и `_check_google_calendar_oauth()` с зафиксированным server-runtime snapshot для `WHOOP/WAZZUP_WEBHOOK_FORWARD/WEBHOOK/Bitrix OAuth/Todoist/Google Calendar OAuth`, чтение локального `logs/system_health.log`
- Результат проверок: целевые тесты зелёные (`Ran 14 tests ... OK`), `py_compile` зелёный. Ручные срезы подтвердили новую классификацию: `WHOOP -> not_checked` с причиной про server-only OAuth-контур, `WAZZUP_WEBHOOK_FORWARD -> ok (server runtime)`, `WEBHOOK -> ok (server runtime)`, `Bitrix OAuth -> not_checked`, если app-state подтверждён в server runtime, `Todoist -> not_checked`, если `todo-integration` подтверждает server-side token/snapshot, `Google Calendar OAuth -> not_checked`, если сам legacy server runtime contour найден. Это убирает ложный класс `Не настроено` для уже заведённых интеграций и переводит их в более честную категорию `ОК` или `Не проверено` в зависимости от глубины probe. В локальном логе `system_health.log` новых ошибок от текущего прогона не появилось; видны только старые записи предыдущих запусков.
- Блокеры: часть `WAZZUP`-сценариев и новые строки `Todoist`/`Google Calendar OAuth` всё ещё зависят от отдельного канонического live-probe; сейчас они честно классифицируются как известный server runtime без глубокой end-to-end валидации
- Следующий шаг: если понадобится ещё точнее, добавить для `Todoist` и `Google Calendar OAuth` не только runtime-detect, но и отдельный read-only smoke/state probe, чтобы часть `not_checked` можно было перевести в подтверждённый `ok`

- Дата и время: `2026-04-18 07:20 MSK`
- Сценарий: `manual`
- Что сделано: локализован источник лишнего вечернего сообщения со скриншота: это не активный cron Ларисы, а legacy server-only `todo-integration` слот `digest:evening` в `/etc/cron.d/openclaw-todo-digest`, который по старому контракту оставался включённым на `19:00 MSK`. В инженерном контуре добавлен явный disable-контракт для этого слота: в [configs/schedule_contract.env](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/configs/schedule_contract.env:1) введён `TODO_DIGEST_EVENING_ENABLED="0"`, а workflow [todo_digest_schedule.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/todo_digest_schedule.sh:1) теперь умеет inspect/apply для `digest:evening` так же, как раньше для `morning/midday`. Заодно обновлены usage/help в [run_workflow.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/run_workflow.sh:1) и архитектурный контракт [schedule_contract.md](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/docs/architecture/schedule_contract.md:1).
- Какие проверки запущены: `bash -n infra/orchestrator/workflows/todo_digest_schedule.sh infra/orchestrator/run_workflow.sh`, `bash checks/instruction_conflicts.sh`, `DRY_RUN=1 make openclaw.todo-digest-schedule MODE=inspect`
- Результат проверок: shell-синтаксис зелёный, `instruction_conflicts` зелёный (`Итог: ОК`). Inspect-flow стартует через штатный orchestrator path, но в текущей локальной сессии падает до SSH-проверки на обязательной переменной `PRIMARY_HOST`; значит repo-fix готов, а live-apply в этой сессии ещё не подтверждён.
- Блокеры: боевое отключение `digest:evening` ещё не применено на сервере из текущей локальной сессии, потому что не поднят remote-ops env (`PRIMARY_HOST` и связанный SSH-контур не подхватились автоматически).
- Следующий шаг: поднять штатный remote-ops env и выполнить `todo_digest_schedule apply`, затем read-only проверить `/etc/cron.d/openclaw-todo-digest`, что `digest:evening` переведён в commented и больше не активен на `19:00 MSK`

- Дата и время: `2026-04-17 07:17 MSK`
- Сценарий: `manual`
- Что сделано: contract checks для утренней sales-рассылки вынесены в CI. В инженерном контуре создан новый GitHub Actions workflow [sales-contract-checks.yml](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/.github/workflows/sales-contract-checks.yml:1), который запускается на `pull_request`, `push` в `main`/`dev`/`codex/**`/`feature/**` и в `workflow_dispatch`. Workflow делает checkout, поднимает Python 3.11 и выполняет два обязательных gate-шага: `python3 -m unittest tests.test_lev_petrovich_runtime tests.test_sales_dispatch_contract -q` и `python3 checks/sales_morning_dispatch_smoke.py`. Это переносит ранее локальные contract checks в PR-контур и делает выпадение `risks` или поломку sequence `sales -> risks -> focus` блокирующей регрессией до merge.
- Какие проверки запущены: `git diff --check -- .github/workflows/sales-contract-checks.yml`, локально `python3 -m unittest tests.test_lev_petrovich_runtime tests.test_sales_dispatch_contract -q`, локально `python3 checks/sales_morning_dispatch_smoke.py`
- Результат проверок: новый workflow файл валиден по diff-check, оба CI-шага зелёные локально (`Ran 50 tests ... OK`, `sales_morning_dispatch smoke OK`). Таким образом sales contract теперь защищён и локально через `post_change_verify`, и в будущем PR pipeline через GitHub Actions
- Блокеры: фактическое срабатывание GitHub Actions ещё не наблюдалось на удалённом PR/run, потому что workflow только что добавлен и локально проверен; для окончательного подтверждения нужен первый реальный run в GitHub
- Следующий шаг: открыть/обновить PR и убедиться, что `Sales Contract Checks` реально стартует и проходит в GitHub UI

- Дата и время: `2026-04-17 07:08 MSK`
- Сценарий: `manual`
- Что сделано: закрыт остаточный guardrail-блокер `instruction_conflicts`, который держал красным общий `post_change_verify`. В [openclaw_healthcheck_schedule.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh:1) в default `STATUS_MESSAGE` добавлена явная защитная формулировка: если есть более свежий run в состоянии `running`, старый `error` не должен объявляться активной проблемой до завершения текущего прогона. Это синхронизировало workflow prompt с проверкой [checks/instruction_conflicts.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/checks/instruction_conflicts.sh:1).
- Какие проверки запущены: `bash checks/instruction_conflicts.sh`, `bash checks/post_change_verify.sh`, `git diff --check -- infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`
- Результат проверок: `instruction_conflicts` зелёный (`Итог: ОК`), общий `post_change_verify` тоже полностью зелёный (`Итог: ОК`). Таким образом после предыдущих правок по утренней sales-рассылке и этой правки guardrail восстановлен полный локальный verification pipeline контура
- Блокеры: отсутствуют по текущей задаче
- Следующий шаг: при необходимости отдельно решить, хотим ли выносить текущие contract checks в CI/PR gate, а не только в локальный `post_change_verify`

- Дата и время: `2026-04-17 07:04 MSK`
- Сценарий: `manual`
- Что сделано: анти-регрессионный план по утренней sales-рассылке реализован в инженерном контуре. Добавлен единый кодовый контракт [agents/sales_agent/report_contract.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/agents/sales_agent/report_contract.py:1) с sequence `sales -> risks -> focus`; runtime [sales_agent.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/agents/sales_agent/sales_agent.py:1), bridge [run_sales_copilot.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/scripts/run_sales_copilot.py:1) и health-check [sales_dispatch_health.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/cloudbot/devops/sales_dispatch_health.py:1) переведены на этот source of truth. Добавлены отдельный dry-run smoke [checks/sales_morning_dispatch_smoke.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/checks/sales_morning_dispatch_smoke.py:1), integration-тесты [tests/test_sales_dispatch_contract.py](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/tests/test_sales_dispatch_contract.py:1) и новые шаги в [checks/post_change_verify.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/checks/post_change_verify.sh:1). Одновременно синхронизирована документация [docs/sales_copilot.md](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/docs/sales_copilot.md:1), где `/risks` больше не описан как alias на `focus`.
- Какие проверки запущены: `python3 -m unittest tests.test_lev_petrovich_runtime tests.test_sales_dispatch_contract -q`, `python3 checks/sales_morning_dispatch_smoke.py`, `bash checks/post_change_verify.sh`, `git diff --check -- agents/sales_agent/report_contract.py agents/sales_agent/sales_agent.py cloudbot/devops/sales_dispatch_health.py scripts/run_sales_copilot.py checks/sales_morning_dispatch_smoke.py checks/post_change_verify.sh tests/test_sales_dispatch_contract.py tests/test_lev_petrovich_runtime.py`
- Результат проверок: целевые sales-проверки зелёные (`Ran 50 tests ... OK`, `sales_morning_dispatch smoke OK`), `git diff --check` чистый. В `post_change_verify` оба новых sales-шага проходят успешно. Итог всего `post_change_verify` всё ещё красный, но только по заранее существующим несвязанным guardrail-проблемам в `instruction_conflicts` (`Status prompt не защищает от ложной эскалации при более свежем run=running`), а не по sales-dispatch контракту.
- Блокеры: общий `post_change_verify` по репозиторию пока не может считаться полностью зелёным, пока не добран legacy-блок `instruction_conflicts`; этот блокер не связан с утренней sales-рассылкой
- Следующий шаг: отдельной задачей разобрать и починить красный `instruction_conflicts`, чтобы вернуть зелёный общий `post_change_verify` всего контура

- Дата и время: `2026-04-17 06:52 MSK`
- Сценарий: `manual`
- Что сделано: после code review sales-контура зафиксирован анти-регрессионный план для утренней рассылки Льва Петровича. Подтверждено, что исторически из пакета выпадал отдельный follow-up отчёт `risks`, то есть пользователь получал только основной `sales` и `focus`. В инженерном контуре уже точечно исправлены runtime health-check, mock bridge и regression-тесты, но на уровне control-plane дополнительно оформлен backlog, чтобы проблема не вернулась через другой слой.
- Какие проверки запущены: ручной review runtime-цепочки `sales -> risks -> focus`, проверка контрольных точек в `engineer`, сверка `PLAN.md` и `STATUS.md` на наличие явных follow-up задач по morning sales dispatch
- Результат проверок: сформирован обязательный checklist дальнейших работ. В него входят: единый source of truth для пакета `sales/risks/focus`, удаление дублирующей follow-up логики из bridge, контрактный integration-test на `/sales`/`/risks`/`/focus-sales`, dry-run smoke утреннего job `morning_sales_dispatch`, усиление health-check по порядку/дубликатам/неполному пакету и включение этих проверок в обязательный post-change verify
- Блокеры: полный репозиторный `checks/smoke_test.py` в инженерном контуре остаётся чувствителен к несвязанным регрессиям соседних маршрутов, поэтому для sales-контракта нужен отдельный целевой smoke, а не только общий smoke всего repo
- Следующий шаг: в инженерном контуре реализовать отдельный contract-smoke для morning sales dispatch и перевести bridge `run_sales_copilot.py` на модель без собственной ручной схемы follow-up

- Дата и время: `2026-04-13 17:35 MSK`
- Сценарий: `manual`
- Что сделано: `midday replan` дополнительно вычищен из активного runtime-контура Ларисы, чтобы не всплывать в расписаниях и operational-help. Из [larisa_agent_deploy.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/larisa_agent_deploy.sh:1) удалены генерация `run_larisa_midday_replan_from_runtime_env.sh`, системный runner `/usr/local/bin/cloudbot-larisa-midday-replan.sh`, cron-подключение и deploy-log строки про midday. Из [run_workflow.sh](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/run_workflow.sh:1) убран `larisa_midday_replan` из usage/help, из [configs/schedules.cron](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/configs/schedules.cron:1) удалён пример слота на `14:00`, из [schedule_contract.md](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/docs/architecture/schedule_contract.md:1) убрано упоминание midday и одновременно зафиксирован актуальный daily slot `08:00 MSK`
- Какие проверки запущены: `bash -n infra/orchestrator/workflows/larisa_agent_deploy.sh`, `bash -n infra/orchestrator/run_workflow.sh`, локально `python3 -m unittest tests.test_larisa_agent`, grep по `configs/`, `docs/`, `infra/orchestrator/` на остаточные упоминания midday
- Результат проверок: синтаксис deploy/runtime-скриптов корректен; тесты проходят (`28 tests, OK`). В активных расписаниях, runtime-help и deploy-контуре `midday replan` больше не фигурирует. Остаточные упоминания сохранены только в ручном техническом коде workflow `infra/orchestrator/workflows/larisa_midday_replan.sh` и связанных agent-командах, как и требовалось
- Блокеры: последние локальные formatter/provider изменения Ларисы ещё не выкатывались отдельным deploy-циклом в active release
- Следующий шаг: при необходимости выкатить актуальный formatter/provider fix Ларисы в `/opt/cloudbot-runtime/larisa/current`

- Дата и время: `2026-04-13 17:20 MSK`
- Сценарий: `manual`
- Что сделано: по запросу пользователя полностью отключена автоматическая отправка `midday replan` Ларисы. В инженерном контракте [configs/schedule_contract.env](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/configs/schedule_contract.env:1) параметр `LARISA_ENABLE_MIDDAY_CRON` переведён в `0`, чтобы последующие deploy не возвращали дневную рассылку. На боевом хосте `ams-1-vm-76ds` переписан `/etc/cron.d/cloudbot-larisa-daily-brief`: из него удалена строка запуска `cloudbot-larisa-midday-replan.sh`, оставлен только daily brief в `08:00 MSK`
- Какие проверки запущены: backup+rewrite live cron-файла `/etc/cron.d/cloudbot-larisa-daily-brief`, повторное чтение live cron-файла, локально `python3 -m unittest tests.test_larisa_agent`
- Результат проверок: на бою cron Ларисы теперь содержит только один автоматический слот `0 5 * * *` (`08:00 MSK`) для утреннего brief; midday-строка отсутствует. Локальные тесты инженерного контура проходят: `28 tests, OK`
- Блокеры: команда `get_midday_replan` и соответствующий workflow остаются в коде как ручной технический маршрут, но автоматически больше не вызываются
- Следующий шаг: при необходимости отдельно вычистить упоминания `midday replan` из внутренних описаний/документации runtime и выкатить последние formatter/provider fixes Ларисы в active release

- Дата и время: `2026-04-13 17:05 MSK`
- Сценарий: `manual`
- Что сделано: зафиксирован новый бизнес-контракт утреннего отчёта Ларисы на `08:00 MSK` вместо `09:00 MSK`. В инженерном контуре обновлён [configs/schedule_contract.env](/Users/pro2kuror/Desktop/OpenClo/projects/engineer/configs/schedule_contract.env:1): `LARISA_DAILY_CRON_MSK="0 8 * * *"` и серверное UTC-выражение `LARISA_DAILY_CRON_EXPR_UTC="0 5 * * *"`. На боевом хосте `ams-1-vm-76ds` синхронно переписан `/etc/cron.d/cloudbot-larisa-daily-brief`: daily brief теперь запускается строкой `0 5 * * *`, что соответствует `08:00 MSK`; midday replan сохранён на `14:00 MSK`
- Какие проверки запущены: чтение локального `configs/schedule_contract.env`, чтение `infra/orchestrator/workflows/larisa_agent_deploy.sh`, backup+rewrite live `/etc/cron.d/cloudbot-larisa-daily-brief`, повторное чтение live cron-файла, локально `python3 -m unittest tests.test_larisa_agent`
- Результат проверок: боевой cron-файл подтверждает новый контракт `08:00 / 14:00 Europe/Moscow`; строка daily на сервере сейчас `0 5 * * *`, строка midday осталась `0 11 * * *`. Локальные тесты инженерного контура проходят: `28 tests, OK`
- Блокеры: форматированный markdown-вариант brief и фильтр лишних Bitrix-встреч уже готовы локально, но ещё не выкачены в active release Ларисы отдельным deploy-циклом
- Следующий шаг: выкатить последний formatter/provider fix в `/opt/cloudbot-runtime/larisa/current` и подтвердить ближайший штатный daily brief в `08:00 MSK`

- Дата и время: `2026-04-13 16:05 MSK`
- Сценарий: `manual`
- Что сделано: выполнен live-fix контура Ларисы на хосте `ams-1-vm-76ds`. В active release `/opt/cloudbot-runtime/larisa/current` обновлён боевой `agents/larisa_ivanovna/formatters/telegram_brief.py`: упрощённый шаблон заменён на управленческий формат с блоками `Календарь дня`, `Просроченные задачи`, `Задачи на сегодня`, `Свободные окна`, `Фокус-блоки`, `Погода`, `3 действия`. Одновременно исправлен live cron-файл `/etc/cron.d/cloudbot-larisa-daily-brief`: вместо выражений, которые фактически отрабатывали на UTC-хосте как `12:00/17:00 MSK`, записаны явные UTC-слоты `0 6 * * *` и `0 11 * * *`, сохраняющие бизнес-контракт `09:00/14:00 MSK`
- Какие проверки запущены: live backup боевого formatter-а и cron-файла; чтение обновлённого `/opt/cloudbot-runtime/larisa/current/agents/larisa_ivanovna/formatters/telegram_brief.py`; чтение обновлённого `/etc/cron.d/cloudbot-larisa-daily-brief`; адресный root-run без Telegram-доставки через `python3 -m agents.larisa_ivanovna --command get_day_brief --date 13.04.2026` с тем же env-path, который использует runtime; локально `bash -n infra/orchestrator/workflows/larisa_agent_deploy.sh`
- Результат проверок: на проде новый formatter уже активен и ручной запуск показывает новый формат brief. Боевой вывод теперь начинается с `Календарь дня`, затем отдельно рендерит `Просроченные задачи` и `Задачи на сегодня`. Адресный root-run подтверждает, что в боевом env доступны ToDo и погодный источник; cron-файл зафиксирован в UTC-выражениях `0 6` и `0 11`, что соответствует `09:00/14:00 MSK`. В инженерном репозитории дополнительно закреплён future-safe deploy path: `configs/schedule_contract.env` и `infra/orchestrator/workflows/larisa_agent_deploy.sh` теперь тоже используют UTC cron expressions для server deploy Ларисы
- Блокеры: для календаря Bitrix на проверенной дате `13.04.2026` live snapshot по-прежнему возвращает `Нет подтвержденных встреч`, поэтому формат восстановлен, но отдельная верификация календаря пользователя Евгений Щемелев на конкретной дате/событиях всё ещё может потребоваться, если пользователь ожидал встречи именно в этот день
- Следующий шаг: дождаться ближайшего штатного прогона `09:00 MSK` и подтвердить, что Лариса шлёт brief уже в новом формате и без сдвига на `12:00 MSK`; если календарь для ожидаемых дат останется пустым, отдельным расследованием пройти owner/profile Bitrix и фактический source state календаря

- Дата и время: `2026-04-13 15:50 MSK`
- Сценарий: `manual`
- Что сделано: проведено live-расследование по утреннему/day brief Ларисы Ивановны. Подтверждено, что сообщение пользователя от `2026-04-13 12:00 MSK` в точности соответствует файлу `/opt/cloudbot-runtime/larisa/current/reports/larisa_daily_brief_20260413_120001_MSK.txt` на боевом хосте `ams-1-vm-76ds`. Причина деградации формата локализована на уровне боевого Python formatter: в active release `/opt/cloudbot-runtime/larisa/releases/codex_feature_self-healing_80ff599` лежит старый `agents/larisa_ivanovna/formatters/telegram_brief.py` с упрощённым шаблоном `Фокус дня / Встречи / Задачи / Свободные окна / Фокус-блоки / Погода / 3 действия`, хотя источники данных ToDo и Bitrix остаются подключёнными. Локально в инженерном контуре подготовлен безопасный fix formatter-а: brief снова делится на `Календарь дня`, `Просроченные задачи`, `Задачи на сегодня`, `Свободные окна`, `Фокус-блоки`, `Погода`, `3 действия`, без потери текущих provider-ов и маршрута `day_briefing`.
- Какие проверки запущены: локально `python3 -m unittest tests.test_larisa_agent`; ручной `python3 -m agents.larisa_ivanovna --command get_day_brief --date 23.03.2026` на fixture ToDo+Bitrix; live SSH через `ops/ssh_happ.sh` с чтением `/etc/cron.d/cloudbot-larisa-daily-brief`, `/usr/local/bin/cloudbot-larisa-daily-brief.sh`, `/usr/local/bin/cloudbot-larisa-midday-replan.sh`, `/opt/cloudbot-runtime/larisa/current/run_larisa_*`, `/opt/cloudbot-runtime/larisa/current/agents/larisa_ivanovna/formatters/telegram_brief.py`, `/opt/cloudbot-runtime/larisa/current/reports/larisa_daily_brief_20260413_120001_MSK.txt`, а также tail `larisa_daily_brief_cron.log` и `larisa_midday_replan_cron.log`
- Результат проверок: локальные тесты проходят (`27 tests, OK`), ручной рендер brief на fixture снова показывает отдельные блоки по календарю, просрочке и задачам на сегодня. На боевом хосте дополнительно обнаружен отдельный scheduler-дефект: несмотря на текущий cron-файл с `CRON_TZ=Europe/Moscow` и выражениями `0 9 * * *` / `0 14 * * *`, фактические логи показывают исполнение `larisa_daily_brief` в `12:00 MSK` и `larisa_midday_replan` в `17:00 MSK`, то есть live-слот Ларисы сейчас смещён на `+3 часа` относительно ожидаемого МСК-контракта
- Блокеры: формат brief локально уже исправлен, но active release на сервере ещё не обновлён; отдельно остаётся не до конца объяснённый cron/runtime drift на `+3 часа`, который нужно поправить до следующего выкатного окна, иначе даже после обновления formatter-а Лариса продолжит слать daily brief в `12:00 MSK`, а midday replan в `17:00 MSK`
- Следующий шаг: подготовить отдельный безопасный rollout для Ларисы: 1) выкатить исправленный formatter в scoped runtime `/opt/cloudbot-runtime/larisa/current`; 2) отдельно исправить live scheduler drift, сверив, почему cron-файл с `CRON_TZ=Europe/Moscow` фактически исполняется как UTC, и после фикса подтвердить боевой запуск именно в `09:00 MSK` и `14:00 MSK`

- Дата и время: `2026-04-12 12:25 MSK`
- Сценарий: `manual`
- Что сделано: выполнен live cutover утренних operational-отчётов на хосте `ams-1-vm-76ds`. Через прямой privileged OpenClaw CLI обновлены боевые job: `healthcheck-daily` оставлен на `09:00 MSK`, но переведён в `delivery.mode=none` как внутренний upstream-check; `daily-status-report` оставлен единственным публичным job на `09:30 MSK` и обновлён до нового единого формата с визуальным каркасом `🟢 СЕРВЕР СТАБИЛЕН` / `🔴 ЕСТЬ ПРОБЛЕМЫ`, полным блоком API/возможностей и правилами без дублей. Отдельно найден и отключён legacy `11:00` telegram-send из root crontab: строка `0 8 * * * /usr/local/sbin/host-security-check.sh` закомментирована, при этом `07:30` server health-check c `--no-telegram` сохранён как внутренний серверный контроль без пользовательской доставки.
- Какие проверки запущены: live SSH preflight на `72.56.83.251` через `ops/ssh_happ.sh`, privileged `openclaw cron list --all --json`, `openclaw cron runs --id ... --limit 3`, чтение `/etc/cron.d/openclaw-todo-digest`, чтение root crontab, grep по live `/etc/cron.d`, `/usr/local/bin`, `/root/.openclaw/workspace`, прямой `cron edit` для `healthcheck-daily` и `daily-status-report`, повторный `cron list --all --json`, backup+rewrite root crontab с сохранением в `/var/backups/root-crontab-unified-morning-report-20260412_092319_MSK.bak`
- Результат проверок: live-state подтверждает `healthcheck-daily.schedule=0 9 * * * Europe/Moscow` и `delivery.mode=none`; `daily-status-report.schedule=30 9 * * * Europe/Moscow` и `delivery.mode=announce`; legacy `openclaw-todo-digest` утренний и midday слоты уже были отключены ранее; root crontab больше не содержит активный `0 8 * * * /usr/local/sbin/host-security-check.sh`, строка закомментирована с пометкой `disabled after unified 09:30 operational report`; таким образом из подтверждённых live-источников публичным утренним operational-отчётом остаётся только `daily-status-report` в `09:30 MSK`
- Блокеры: утренний unified report в новом формате ещё не наблюдался в следующем штатном боевом прогоне после правки, поэтому окончательное подтверждение теперь зависит только от следующего слота `09:30 MSK`
- Следующий шаг: после ближайшего планового прогона подтвердить фактическую доставку одного сообщения в `09:30 MSK` и убедиться, что отдельные `09:00` и `11:00` operational-дубли больше не приходят

- Дата и время: `2026-04-12 11:59 MSK`
- Сценарий: `manual`
- Что сделано: добран остаточный smoke-failure после удаления VPN-контура. В `scripts/run_sales_copilot.py` исправлена логика bridge Sales Copilot: при `SALES_COPILOT_MOCK=1` mock-ветка теперь действительно автономна и не пытается заранее читать remote env/state по SSH. Это возвращает корректный payload для `/sales` с `parse_mode=HTML` и отдельным вторым сообщением `Фокус РОПа`.
- Какие проверки запущены: `python3 -m py_compile scripts/run_sales_copilot.py`, адресная проверка `handle_update('/sales')` в mock-режиме, полный `python3 checks/smoke_test.py`, затем `git diff --check` в `engineer` и `architect`
- Результат проверок: адресный payload `/sales` теперь содержит `workflow=sales_brief`, `parse_mode=HTML` и два `message_chunks`; полный smoke проходит целиком (`SMOKE TEST OK`); дополнительных регрессий после фикса не обнаружено
- Блокеры: отсутствуют по текущей задаче
- Следующий шаг: при необходимости отдельно вынести mock/live contract Sales Copilot в явный тест, чтобы подобный ранний SSH-path больше не ломал локальные smoke-проверки

- Дата и время: `2026-04-12 14:55 MSK`
- Сценарий: `manual`
- Что сделано: из инженерного и control-plane контуров полностью удалён устаревший VPN-контур и все прямые привязки к `infra/happ-vpn.env`, `sing-box`, `vpn_verify`, `vpn_smoke_happ`, `morning_health_report`, `runbook_happ_vpn`, `architecture_happ_vpn` и связанным workflow/скриптам. Взамен закреплён нейтральный operational env `infra/remote-ops.env` и его пример `infra/remote-ops.env.example`. Для безболезненной совместимости удалённого доступа `ops/ssh_happ.sh` и `checks/check_access.sh` теперь принимают `PRIMARY_HOST` как основной source of truth, но также подхватывают legacy alias `OPENCLAW_HOST`, если он ещё присутствует во внешнем окружении.
- Какие проверки запущены: полнотекстовый поиск по двум workspace через `rg` и `find` на остатки `happ-vpn.env`, `VPN`, `sing-box`, `.vpn-client` и старых vpn-workflow; `python3 -m py_compile` для `cloudbot/devops/system_health.py`, `cloudbot/workflows/system_health.py`, `checks/smoke_test.py`, `scripts/run_sales_copilot.py`; `bash -n` для `infra/orchestrator/run_workflow.sh`, `ops/ssh_happ.sh`, `checks/check_access.sh`, `checks/instruction_conflicts.sh`, `infra/orchestrator/workflows/daily_ops.sh`, `infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`; `git diff --check` в `engineer` и `architect`; ручной dry-run `checks/check_access.sh` и `run_workflow.sh openclaw_healthcheck_schedule inspect`
- Результат проверок: прямых следов старого VPN-контура в репозиториях больше не найдено; bash/python синтаксис и `git diff --check` проходят; удалённый контур на уровне кода переведён на `remote-ops`. Dry-run показал не функциональную поломку, а отсутствие локально заданного host/env для live-операций, что ожидаемо без реального `infra/remote-ops.env`.
- Блокеры: в локальном workspace по-прежнему отсутствует реальный `infra/remote-ops.env` с боевым `PRIMARY_HOST`, поэтому live SSH-проверка и inspect боевых OpenClaw job из этого рабочего дерева не выполняются. Это не след старого VPN-контура, а просто отсутствие рабочего operational env-файла вне git.
- Следующий шаг: при необходимости положить актуальный `infra/remote-ops.env` вне git и отдельно прогнать live `ssh.preflight` и `openclaw.healthcheck-schedule` уже на боевом host через новый remote-ops контур

- Дата и время: `2026-04-12 14:10 MSK`
- Сценарий: `manual`
- Что сделано: формализован канонический контракт единого утреннего operational-отчёта Ларисы Ивановны. В `docs/architecture/morning_operational_report_contract.md` зафиксированы: один итоговый утренний отчёт вместо трёх дублей, визуальный каркас `🟢 СЕРВЕР СТАБИЛЕН` / `🔴 ЕСТЬ ПРОБЛЕМЫ`, обязательные блоки `Хост / OpenClaw / API / Пользовательские возможности / Планировщик / Безопасность`, правила для `Новые API` и раздельная трактовка `API` против `user-facing capability`. Отдельно задокументирован принцип для web search: `provider`, `skill` и `Web search для Ларисы` — это разные статусы и они не должны сваливаться в один ложный `OK`.
- Какие проверки запущены: анализ текущих control-plane документов `docs/prompts/daily-health-check.md`, `docs/checklists/health-check.md`, live-reading инженерного контура `cloudbot/devops/system_health.py`, `cloudbot/providers/search_provider.py`, `cloudbot/skills/web_search.py`, `cloudbot/orchestrator/router.py`, `agents/larisa_ivanovna/agent.py`, затем синхронизация control-plane документов и проверка patch на согласованность
- Результат проверок: подтверждено документально и кодовым чтением, что текущий health-check не проверяет web search как пользовательскую возможность, а существующий search-path в инженерном контуре сейчас допускает ложноположительный `OK` на уровне provider/skill stub; control-plane контракт теперь явно запрещает такую интерпретацию и требует отдельного capability-статуса `Web search для Ларисы`
- Блокеры: это пока control-plane спецификация, а не инженерное внедрение. Для полного исправления поведения нужно отдельной задачей обновить runtime-checks и рендер отчёта в `/Users/pro2kuror/Desktop/OpenClo/projects/engineer`, чтобы новые статусы считались не по заглушкам, а по реальному smoke user-facing path
- Следующий шаг: в инженерном контуре внедрить новый report renderer и реальные health/capability probes для `Web Search provider`, `web_search skill` и `Web search для Ларисы`, а также перевести давно известные интеграции из daily `Новые API` в основной штатный реестр

- Дата и время: `2026-04-03 17:35 MSK`
- Сценарий: `manual`
- Что сделано: для Google Sheet `Копия Сбор Чеков Самозанятых` добавлен отдельный нормализационный слой сотрудников. В инженерном контуре создан script `scripts/finansist_build_employee_dictionary.mjs`, который собирает сотрудников за `2024/2025/2026`, строит лист `Справочник сотрудников` и лист `Сомнительные совпадения`. Подтвержденный вручную кейс `Тартышева Валерия Михайловна` ↔ `Тартышева Валерия Михайлонва` записан как один сотрудник. Одновременно `scripts/finansist_build_fot_analytics.mjs` обновлен так, чтобы аналитика ФОТ использовала `Справочник сотрудников` как слой нормализации, а не только raw-имена из месячных вкладок.
- Какие проверки запущены: `node --check scripts/finansist_build_employee_dictionary.mjs`, `node --check scripts/finansist_build_fot_analytics.mjs`, live apply `scripts/finansist_build_employee_dictionary.mjs --sheet-url ...`, live apply `scripts/finansist_build_fot_analytics.mjs --sheet-url ...`, контрольное live-read через `scripts/finansist_google_read_ranges.mjs` по диапазонам `Справочник сотрудников!A1:E8`, `Сомнительные совпадения!A1:E5`, `Финансовая аналитика ФОТ 2025–2026!A1:M3`
- Результат проверок: в таблице созданы листы `Справочник сотрудников` и `Сомнительные совпадения`; в справочник попало `251` raw-вариант имени, сомнительных совпадений осталось `1`, и он помечен как `Подтверждено вручную: один сотрудник`; аналитический лист успешно пересобран и теперь явно указывает источники `месячные листы 2025/2026` и `Справочник сотрудников`; live-read подтверждает наличие новых листов и актуальной подписи источника
- Блокеры: часть записей в raw-данных не являются чистыми ФИО (`Видеограф Вольво Тушино`, `Фотосьемка Вилмадент`, одиночные фамилии и т.п.), поэтому они нормализованы технически, но для управленческого качества модели позже стоит отдельно ввести признак `тип записи: сотрудник / подрядчик / служебная строка`
- Следующий шаг: при необходимости 1) добавить в справочник поле `статус/тип записи`, 2) вынести нормализованный raw-кэш сотрудников в отдельный helper-лист для дальнейших отчетов по переходам и динамике ФОТ, 3) расширить employee-аналитику на новые/ушедшие/сменившие отдел уже поверх этого справочника

- Дата и время: `2026-04-03 17:05 MSK`
- Сценарий: `manual`
- Что сделано: для Google Sheet `Копия Сбор Чеков Самозанятых` создана управленческая аналитика ФОТ: лист `Финансовая аналитика ФОТ 2025–2026` и лист `Сопоставление отделов`. После первичной ошибки с source-of-truth расчёт был переведён с листа `данные` на реальные месячные вкладки `2025/2026`, затем добавлен устойчивый helper-кэш в аналитическом листе и пересобраны сводные формулы, доли, средние значения и управленческое сопоставление отделов по сотрудникам и смыслу названий.
- Какие проверки запущены: live inspect структуры через `scripts/finansist_google_sheet_inspect.mjs`, несколько live apply-прогонов `scripts/finansist_build_fot_analytics.mjs`, точечные readback-проверки `scripts/finansist_google_read_ranges.mjs` по диапазонам `Сопоставление отделов` и `Финансовая аналитика ФОТ 2025–2026`
- Результат проверок: листы созданы и читаются live; в `Сопоставление отделов` подтверждены пары вроде `Департамент продаж -> Отдел продаж`, `SEO -> Отдел SEO`, `Администрация -> Административный отдел`; на сводном листе появились ненулевые показатели и расчеты по 2025/2026, например `Администрация: 6 457 510 vs 2 279 747`, `Аккаунтинг: 7 729 970 vs 2 789 485`, средние значения и доли больше не падают в `#VALUE!`
- Блокеры: источник подписи на сводном листе всё ещё текстом говорит про лист `данные`, хотя фактический расчёт уже идёт по месячным листам; общий управленческий блок инсайтов сейчас строится как generated summary на момент сборки, а не как полностью формульный self-update слой
- Следующий шаг: при необходимости довести витрину до идеала: 1) обновить поясняющий текст про source-of-truth, 2) усилить автоматические инсайты и списки сотрудников на листе, 3) при желании добавить отдельный лист с нормализованным raw-кэшем 2025/2026 вместо helper-области внутри аналитического листа

- Дата и время: `2026-04-03 16:20 MSK`
- Сценарий: `manual`
- Что сделано: найден реальный Google service-account credential path для финансового контура: `/Users/pro2kuror/Downloads/finance-director-sheets-903611b799c3.json`. Для `Финансиста` добавлен отдельный live smoke `checks/finansist_google_smoke.mjs`, который без внешних SDK подписывает JWT сервисным аккаунтом, получает OAuth token и ходит в Google Sheets/Docs API. Живой smoke на боевом sheet `Копия ДДС_2026` выполнен успешно.
- Какие проверки запущены: поиск credential path по машине (`rg`, `find`), `node --check checks/finansist_google_smoke.mjs`, локальный запуск smoke с sandbox, повторный live запуск вне sandbox: `GOOGLE_SERVICE_ACCOUNT_JSON=/Users/pro2kuror/Downloads/finance-director-sheets-903611b799c3.json node checks/finansist_google_smoke.mjs --sheet-url 'https://docs.google.com/spreadsheets/d/15EYS6EtWrQt8vJWLZ52QXDGKNceSApqgmXkjMmkJb9g/edit?gid=279576169#gid=279576169'`
- Результат проверок: credential path найден и соответствует сервисному аккаунту `finance-director-sheets-bot@finance-director-sheets.iam.gserviceaccount.com`; live Google Sheets smoke вернул `Google Sheets OK: Копия ДДС_2026 (tabs=42)`; значит доступ `Финансиста` к этому Google Sheet подтвержден фактическим API-read, а не только кодовой конфигурацией
- Блокеры: live read для Google Docs как отдельного типа источника пока не подтвержден, потому что в этом сеансе дана только ссылка на Google Sheet, а не на Google Doc
- Следующий шаг: получить одну боевую ссылку на Google Doc и прогнать `checks/finansist_google_smoke.mjs --doc-url ...`, после чего доступ `Финансиста` к Google Docs и Google Sheets будет подтвержден полностью

- Дата и время: `2026-04-03 12:55 MSK`
- Сценарий: `manual`
- Что сделано: финансовый контур усилен до уровня проверяемого runtime-path. В `projects/engineer` исправлен перенос метрик/источников через orchestrator для `Финансиста`, а в `checks/smoke_test.py` добавлен новый шаг `/finance`, который проверяет route `finance_summary`, управленческий вывод и явное отображение статуса `Google Docs` / `Google Sheets`.
- Какие проверки запущены: `python3 -m py_compile cloudbot/workflows/finance_runtime.py tests/test_finansist_agent.py checks/smoke_test.py`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 -m unittest tests/test_finansist_agent.py`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 checks/smoke_test.py`
- Результат проверок: `py_compile` зелёный; `tests/test_finansist_agent.py` проходит (`Ran 6 tests ... OK`); новый orchestrator smoke для `/finance` проходит и подтверждает route + управленческий текст + явный статус `Google Docs/Sheets`; общий `checks/smoke_test.py` падает позже на уже существующей проблеме чужого контура: `Команда /sales не выставила HTML parse mode`
- Блокеры: для `Финансиста` остаётся live-блокер по Google credential env; дополнительно общий smoke репозитория сейчас красный из-за несвязанной регрессии sales-контура `/sales`
- Следующий шаг: 1) дать/подтвердить Google credential path и реальный Google Doc/Sheet для live smoke `Финансиста`; 2) отдельно разобрать и починить existing-regression в `/sales`, чтобы вернуть зелёный общий smoke всего репозитория

- Дата и время: `2026-04-03 12:35 MSK`
- Сценарий: `manual`
- Что сделано: в инженерном контуре `/Users/pro2kuror/Desktop/OpenClo/projects/engineer` создан новый runtime-пакет `agents/finansist/*` как отдельный финансовый контур, не смешанный с Ларисой Ивановной и sales-контуром. Добавлены `agent.yaml`, `prompt.md`, CLI/runtime entrypoint, отдельные finance-workflow в `cloudbot/workflows/*`, router/intents и Telegram-команды `/finance`, `/pnl`, `/cashflow`, `/runway`, `/expenses`, `/clients-profit`, `/ar`, `/ap`, `/finance-risks`. В source-layer отдельно добавлена read-only поддержка `Google Docs` и `Google Sheets`: контур распознает оба типа URL, учитывает catalog JSON и явно пишет, если credential path для Google Workspace не подтвержден.
- Какие проверки запущены: `env | cut -d= -f1 | rg 'GOOGLE|SHEET|DRIVE'`, `python3 -m py_compile` по новым модулям `agents/finansist/*`, `cloudbot/workflows/finance_*`, `python3 -m unittest tests/test_finansist_agent.py`, ручной CLI-прогон `python3 -m agents.finansist --command get_finance_summary --payload-json ...` с `docs.google.com/document` и `docs.google.com/spreadsheets` URL
- Результат проверок: синтаксис зелёный; `tests/test_finansist_agent.py` проходит (`Ran 5 tests ... OK`); ручной прогон подтверждает, что `Финансист` корректно распознаёт `Google Docs` и `Google Sheets`, строит управленческий ответ и отдельно показывает отсутствие credential path; названия Google env-переменных в текущем локальном окружении не найдены, значит live-доступ к ранее созданным Google Docs/Sheets в этом runtime пока не подтвержден
- Блокеры: в текущем локальном runtime нет подтвержденного Google credential env (`GOOGLE_WORKSPACE_CREDENTIALS_JSON`, `GOOGLE_DRIVE_CREDENTIALS_JSON`, `GOOGLE_SERVICE_ACCOUNT_JSON` или `GOOGLE_OAUTH_CREDENTIALS_JSON`), поэтому доступ к ранее созданным Google Docs/Sheets заложен в коде, но не может считаться live-ready без настройки env/секрета
- Следующий шаг: завести/подтвердить Google credential path для runtime, затем прогнать live smoke-check `Финансиста` на реальном Google Doc и Google Sheet и после этого подключить Bitrix24/Excel/CSV как фактические owner-источники

- Дата и время: `2026-04-03 12:00 MSK`
- Сценарий: `manual`
- Что сделано: подготовлен отдельный control-plane пакет для нового финансового контура `Финансист`. Добавлен документ `docs/architecture/finansist_contour.md` с целевой архитектурой, границами ответственности, route/intent/persona-схемой, списком сущностей, источников данных, обязательных метрик, базовых workflow, рисков данных и пошаговым планом запуска. Отдельно добавлен `docs/prompts/finansist_system_prompt.md` с системным prompt для управленческого финансового агента в логике собственника digital-агентства.
- Какие проверки запущены: визуальная сверка структуры существующих agent-контуров в `/Users/pro2kuror/Desktop/OpenClo/projects/engineer`, просмотр `cloudbot/orchestrator/router.py`, `docs/architecture/agent_boundaries.md`, `infra/orchestrator/workflows/excel_opiu_reconcile.sh`, ручная проверка новых markdown-файлов, `git diff --check`
- Результат проверок: спецификация стыкуется с текущей картой инженерного контура и не смешивает `Финансиста` с Ларисой Ивановной, Львом Петровичем и `Архитектором`; конфликтов форматирования по `git diff --check` нет
- Блокеры: live runtime-контур `Финансиста` в `/Users/pro2kuror/Desktop/OpenClo/projects/engineer` ещё не создан в этом сеансе; текущий результат — архитектурный и prompt-пакет для последующего инженерного внедрения без deploy
- Следующий шаг: по отдельной инженерной задаче создать runtime-пакет `agents/finansist/*`, добавить router/intents/workflows в `projects/engineer` и прогнать smoke-check по Google Sheets, Excel и Bitrix24 read-only

- Дата и время: `2026-04-02 16:45 MSK`
- Сценарий: `manual`
- Что сделано: в контур черновиков добавлены быстрые команды управления тоном: `/harder`, `/softer`, `/business`. Они не создают отдельные workflow, а переключают режим внутри существующего `get_content_post`, поэтому тема, номер поста и логика фактуры остаются едиными, а меняется только подача текста. `harder` делает заход и выводы жёстче, `softer` смягчает тон и делает подачу спокойнее, `business` переводит акцент на управляемость, масштабирование и стоимость сопровождения.
- Какие проверки запущены: `python3 -m py_compile` для `schemas/content.py`, `formatters/telegram_content_post.py`, `commands/get_content_post.py`, `workflows/content_topics.py`, `cloudbot/workflows/larisa_runtime.py`, `agents/larisa_ivanovna/agent.py`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 -m unittest tests/test_larisa_agent.py`, ручные прогоны `python3 -m agents.larisa_ivanovna --command get_content_post --period day --topic 1 --tone harder|softer|business`
- Результат проверок: `py_compile` зелёный; `tests/test_larisa_agent.py` проходит (`Ran 26 tests ... OK`); ручные прогоны подтверждают, что три режима действительно меняют формулировки hook/body и не сводятся к простому label
- Блокеры: live Telegram-доставка быстрых tone-команд в этом сеансе не подтверждена из-за сетевого sandbox, но routing/parse-path и ручные CLI-прогоны подтверждены
- Следующий шаг: если потребуется, добавить ещё два режима подачи (`personal`, `case`) или сделать повторную генерацию по той же теме через короткие команды вида `/harder 2` прямо в чате

- Дата и время: `2026-04-02 16:39 MSK`
- Сценарий: `manual`
- Что сделано: контур копирайтера дополнительно адаптирован под стиль Telegram-канала `belberry_marketing` после анализа публичной витрины канала. Шаблоны тем и черновиков переписаны с упором на личный голос, неудобный тезис в начале, управленческий/операционный угол, короткие практические выводы и менее “технический changelog”-тон. Теперь темы формулируются ближе к редакционному паттерну канала, а `get_content_post` строит более живой черновик с заходом в духе личного наблюдения, а не сухого инженерного отчёта.
- Какие проверки запущены: просмотр публичной web-витрины канала `https://t.me/s/belberry_marketing`, `python3 -m py_compile agents/larisa_ivanovna/workflows/content_topics.py`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 -m unittest tests/test_larisa_agent.py`, ручные прогоны `python3 -m agents.larisa_ivanovna --command get_content_topics --period day` и `python3 -m agents.larisa_ivanovna --command get_content_post --period day --topic 1`
- Результат проверок: публичная витрина подтвердила нужный тон канала; `py_compile` зелёный; `tests/test_larisa_agent.py` проходит (`Ran 25 tests ... OK`); ручной `get_content_topics` теперь выдаёт темы в более редакционном стиле канала, а `get_content_post` строит черновик с сильным хуком, личным голосом и практическим выводом
- Блокеры: анализ канала выполнен по публичной web-витрине Telegram, поэтому для ещё более точной стилизации полезно позже взять 10-15 эталонных постов напрямую из полной ленты канала
- Следующий шаг: при желании отдельно ввести несколько режимов тона для черновиков (`жёстче`, `спокойнее`, `больше кейса`, `больше управленческого вывода`) и подстроить стиль под конкретные рубрики канала

- Дата и время: `2026-04-02 15:35 MSK`
- Сценарий: `manual`
- Что сделано: контур копирайтера Ларисы Ивановны расширен до рабочего daily-loop. В `configs/schedules.cron` добавлен ежедневный слот `19:30 МСК` для рассылки тем за день. В агент добавлена новая команда `get_content_post` и Telegram-маршруты `/draft`, `/write-post`, а также естественная команда `пост 2` для генерации черновика по номеру темы. Workflow `content_topics` теперь собирает фактуру не только из `git` и `docs/STATUS.md`, но и из задач через `TodoistTasksProvider` и из PR через `gh pr list` с безопасным fallback при недоступности CLI/авторизации.
- Какие проверки запущены: `python3 -m py_compile` для новых и изменённых Python-модулей, `bash -n infra/orchestrator/workflows/larisa_content_topics.sh infra/orchestrator/run_workflow.sh`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 -m unittest tests/test_larisa_agent.py`, ручные прогоны `python3 -m agents.larisa_ivanovna --command get_content_topics --period day`, `python3 -m agents.larisa_ivanovna --command get_content_post --period day --topic 1`, operational-path проверка `REPORT_DIR=/tmp PERIOD=day ./infra/orchestrator/run_workflow.sh larisa_content_topics`
- Результат проверок: `py_compile` зелёный, shell syntax-check зелёный, `tests/test_larisa_agent.py` проходит (`Ran 25 tests ... OK`), ручной `get_content_topics` возвращает темы и корректно показывает ограничения по отсутствующим задачам/PR, `get_content_post --topic 1` строит черновик поста с хуком, планом и телом текста, `run_workflow.sh larisa_content_topics` успешно формирует отчёт через orchestrator path
- Блокеры: в текущем Codex-сеансе сетевой sandbox по-прежнему не позволяет подтвердить live-вызов `gh` и реальную доставку в Telegram; поэтому PR-ветка и Telegram-доставка в ручных локальных прогонах подтверждены только логикой graceful fallback, а не живым API-ответом
- Следующий шаг: на живом контуре без sandbox проверить, что `gh pr list` даёт фактуру в темах, затем подтвердить первую автоматическую отправку слота `19:30 МСК` и при необходимости подправить стиль черновиков под конкретный канал

- Дата и время: `2026-04-02 15:09 MSK`
- Сценарий: `manual`
- Что сделано: в инженерном контуре реализован MVP контур копирайтера внутри Ларисы Ивановны. Добавлена новая команда `get_content_topics` с Telegram-алиасами `/topics`, `/posts`, `/ideas`, отдельный workflow `agents/larisa_ivanovna/workflows/content_topics.py`, formatter для Telegram и shell-workflow `infra/orchestrator/workflows/larisa_content_topics.sh`. Контур собирает фактуру из локального `git` и `docs/STATUS.md`, предлагает темы для постов за день, неделю или весь период и не отправляет пустые темы при отсутствии подтверждённых сигналов.
- Какие проверки запущены: `python3 -m py_compile` для новых Python-модулей, `bash -n infra/orchestrator/workflows/larisa_content_topics.sh infra/orchestrator/run_workflow.sh`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && python3 -m unittest tests/test_larisa_agent.py`, ручной запуск `python3 -m agents.larisa_ivanovna --command get_content_topics --period day`, operational-path проверка `REPORT_DIR=/tmp ./infra/orchestrator/run_workflow.sh larisa_content_topics`
- Результат проверок: `py_compile` зелёный, shell syntax-check зелёный, `tests/test_larisa_agent.py` проходит (`Ran 24 tests ... OK`), ручной запуск возвращает корректный digest и при отсутствии фактуры сообщает, что сильных тем за день нет; `run_workflow.sh larisa_content_topics` успешно отрабатывает end-to-end при `REPORT_DIR=/tmp`, отчёт формируется корректно
- Блокеры: в текущем Codex-сеансе сетевой sandbox не даёт проверить реальную доставку в Telegram (`urlopen error ... nodename nor servname provided`), а штатная запись отчёта в `engineer/reports` также ограничена sandbox и для локальной проверки была перенаправлена во временный каталог `/tmp`
- Следующий шаг: на живом контуре без sandbox подтвердить доставку в Telegram через существующий маршрут Ларисы Ивановны и затем, если нужно, включить ежедневный слот автоматической отправки тем

- Дата и время: `2026-04-01 09:43 MSK`
- Сценарий: `manual`
- Что сделано: разобран ложный тревожный статус в утреннем отчёте Ларисы Ивановны. Подтверждено live-read-only inspect, что `daily-status-report` уже завершился `ok` в `2026-04-01 09:30:00 MSK`, а показанное сообщение было промежуточной эскалацией на фоне старого `error` и ещё идущего текущего `running` run. В инженерном контуре усилен prompt `infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`: теперь перед объявлением active problem нужно перепроверять самый свежий run того же job, трактовать более свежий `ok`/`delivered` как self-healed случай и не объявлять старый `error` активной поломкой, пока новый run ещё `running`. В control-plane синхронизированы `docs/prompts/daily-health-check.md` и `docs/checklists/health-check.md`, а в `checks/instruction_conflicts.sh` добавлена защита от регресса этой формулировки.
- Какие проверки запущены: live `./ops/ssh_happ.sh primary "cd /opt/openclaw ... cron list --all --json"`, `bash -n /Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`, `bash -n /Users/pro2kuror/Desktop/OpenClo/projects/engineer/checks/instruction_conflicts.sh`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && bash checks/instruction_conflicts.sh`, просмотр diff в `engineer` и `architect`
- Результат проверок: live-state подтверждает `healthcheck-daily=ok` в `2026-04-01 09:00:00 MSK`, `daily-status-report=ok` в `2026-04-01 09:30:00 MSK`, delivery прошла, `consecutiveErrors=0`, следующий запуск статуса запланирован на `2026-04-02 09:30:00 MSK`; shell syntax-check зелёный; `instruction_conflicts.sh` вернул `Итог: ОК`; документированные guardrails теперь явно покрывают случай `older error + newer running`
- Блокеры: боевой payload на сервере пока не переапплаен этой новой формулировкой, поэтому до отдельного `apply` live-job всё ещё использует прежний текст prompt; как техдолг остаётся mismatch версий OpenClaw config/runtime (`2026.3.13` vs `2026.3.8`)
- Следующий шаг: безопасным окном выполнить `openclaw.healthcheck-schedule MODE=apply` для доставки обновлённого prompt на live-хост и на следующем утреннем прогоне подтвердить, что ложная эскалация больше не повторяется

- Дата и время: `2026-03-29 17:48 MSK`
- Сценарий: `manual`
- Что сделано: закрыт remaining operational contour по flaky OpenClaw CLI/gateway. В `engineer/ops/ssh_happ.sh` канонический SSH-wrapper переведён на `bash -lc` для remote-команд, чтобы server env и startup-переменные OpenClaw применялись одинаково в batch-path. В workflow `openclaw_healthcheck_schedule.sh`, `openclaw_update.sh`, `openclaw_update_permissions.sh`, `openclaw_gateway_repair.sh`, `openclaw_backup_schedule.sh` приоритет вызова переключён на глобальный `openclaw` binary с fallback на raw `node dist/entry.js`; это убирает residual first-call flakiness из боевого ops-path. На сервере через `openclaw_gateway_repair apply` уже закреплены `NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache` и `OPENCLAW_NO_RESPAWN=1` в `/opt/openclaw/.env` и `docker-compose.yml`. Дополнительно исправлены и применены два server helper path: `openclaw_update_permissions` теперь корректно работает при отсутствии пользователя `node` и выдаёт helper для существующего `ops`, а `openclaw_backup_schedule` больше не использует неинициализированные переменные и перегенерировал `/usr/local/sbin/openclaw-backup.sh` на wrapper-first path
- Какие проверки запущены: `bash -n ops/ssh_happ.sh` и обновлённых workflow, live `./ops/ssh_happ.sh primary` с quoted/multiline remote-командами, серии `openclaw cron list --all --json` и `node dist/entry.js cron list --all --json`, `DRY_RUN=0 make openclaw.healthcheck-schedule MODE=inspect`, `DRY_RUN=0 ./infra/orchestrator/run_workflow.sh openclaw_gateway_repair inspect`, `openclaw_update inspect`, `openclaw_update_permissions apply`, `openclaw_backup_schedule apply`, live запуск `sudo -n /usr/local/sbin/openclaw-update-helper status`
- Результат проверок: `make openclaw.healthcheck-schedule MODE=inspect` теперь проходит без первого non-JSON флапа; `openclaw_gateway_repair inspect` и `openclaw_update inspect` проходят штатно; helper `/usr/local/sbin/openclaw-update-helper` установлен, sudoers для `ops` валиден; backup script обновлён и test-run создал новый архив `/var/backups/openclaw/2026-03-29T14-47-57.743Z-openclaw-backup.tar.gz` с `verified=true`. Сырой `node dist/entry.js` path на хосте всё ещё может дать единичный `gateway closed (1000)` при прямом обходе wrapper/ops-workflow, но канонический operational path Cloudbot/OpenClaw переведён в стабильное состояние
- Блокеры: активных блокеров по Cloudbot ops-path нет; как техдолг OpenClaw остаётся несовпадение версий config/runtime (`config written by newer OpenClaw 2026.3.13`, local CLI `2026.3.8`) и upstream cold-start bug у raw `dist/entry.js`
- Следующий шаг: подтвердить утренний боевой прогон `2026-03-30 09:00 MSK` и `09:30 MSK` без дублей, затем разнести локальные изменения по чистым коммитам и при отдельном окне обслуживания обновить сам OpenClaw до актуальной версии

- Дата и время: `2026-03-29 17:27 MSK`
- Сценарий: `manual`
- Что сделано: перепроверен live-доступ через канонический SSH-контур проекта и разобрана точная причина flaky OpenClaw gateway. Подтверждено, что боевой SSH path жив и идёт через operational env с `PRIMARY_HOST=72.56.83.251` и `ops/ssh_happ.sh`, который выводит хост `ams-1-vm-76ds`; прежний вывод про недоступность `95.216.242.28` был следствием обращения в неконечный/устаревший IP-path вне проектного wrapper’а. На live-хосте собрана детальная диагностика `openclaw_gateway_repair inspect`: права и bind-mount на `/root/.openclaw` и `/home/node/.openclaw` корректны, `fs.watch` внутри контейнера проходит, `tools.web.search.provider=kimi` уже зафиксирован, но `cron list` без tuned-env всё ещё флапает на первых попытках с `gateway closed (1000)` / `handshake timeout`. Отдельной live-проверкой доказано, что при `NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache` и `OPENCLAW_NO_RESPAWN=1` первый же `node dist/entry.js cron list --all --json` стабильно проходит. После этого workflow-path в `engineer` обновлён: `openclaw_healthcheck_schedule.sh`, `openclaw_update.sh`, `openclaw_update_permissions.sh`, `openclaw_gateway_repair.sh`, `openclaw_backup_schedule.sh` теперь запускают OpenClaw CLI через compile cache и с отключённым self-respawn
- Какие проверки запущены: `bash checks/check_access.sh`, live `./ops/ssh_happ.sh primary 'node dist/entry.js cron list --all --json'` сериями без tuned-env и с tuned-env, `DRY_RUN=0 ./infra/orchestrator/run_workflow.sh openclaw_gateway_repair inspect`, `DRY_RUN=0 make openclaw.healthcheck-schedule MODE=inspect`, `bash -n` для обновлённых workflow-скриптов
- Результат проверок: SSH-доступ подтверждён; root cause flaky path локализован не в правах и не в mount, а в startup overhead CLI/gateway handshake на small VM. С tuned-env первый вызов проходит; после фикса workflow-path остаётся обратимым и рабочим, но `openclaw_healthcheck_schedule inspect` всё ещё переживает единичные первые non-JSON ответы через встроенный retry, то есть cold-start полностью не исчез, а переведён в управляемую деградацию
- Блокеры: residual first-call flakiness у самого OpenClaw runtime на хосте остаётся; для полного порядка нужно либо закрепить те же env в каноническом server-side launch path OpenClaw, либо дождаться upstream-исправления startup/handshake window
- Следующий шаг: приоритизировать server-side закрепление `NODE_COMPILE_CACHE` / `OPENCLAW_NO_RESPAWN` в каноническом runtime path OpenClaw и после этого повторно измерить серию первых `cron list` без workflow retry

- Дата и время: `2026-03-29 12:16 MSK`
- Сценарий: `manual`
- Что сделано: добраны оставшиеся локальные operational/hygiene задачи без риска для продакшена. Подтверждено, что полный поиск `find /Users/pro2kuror/Desktop -name .git -type d` даёт только три git-контура: `architect`, канонический `projects/engineer` и архивный `archive/restored-workspace`; это добавлено в `engineer/docs/architecture/system_map.md`. В `engineer` усилен weather-контур Ларисы: `OpenMeteoWeatherProvider` теперь делает повторные попытки и отправляет явный `User-Agent`, чтобы transient SSL/network ошибки не роняли погодный блок с первой попытки. Контур `verify_integrations` разделён на два явных entrypoint’а: `scripts/verify_local_preflight.sh` для ноутбука инженера и `scripts/verify_live_integrations.sh` для боевого/env-контура; новые вызовы протянуты через `Makefile`, `devops/health_check.sh`, `infra/orchestrator/workflows/daily_ops.sh` и `cloudbot_architecture_refactor.sh`. Одновременно добавлены hygiene-ignore правила для `.codex`, `.serena`, `cache/logs/reports/tmp` и `.DS_Store`, а legacy tracked-артефакты `.DS_Store` и два `tmp/spreadsheets/*.py` сняты с git-учёта
- Какие проверки запущены: `python3 -m unittest tests.test_larisa_agent`, `bash -n scripts/verify_integrations.sh scripts/verify_local_preflight.sh scripts/verify_live_integrations.sh`, `DRY_RUN=1 ./scripts/verify_local_preflight.sh /tmp/cloudbot-local-preflight`, `git check-ignore -v .codex .serena cache logs reports tmp`, `find /Users/pro2kuror/Desktop -name .git -type d | sort`
- Результат проверок: `23 tests OK`; shell-скрипты проходят syntax-check; `verify_local_preflight` теперь завершает локальный прогон статусом `ОК` и корректно помечает отсутствующие боевые секреты как `SKIP`, а не как красную поломку; `git check-ignore` подтверждает, что `.codex`, `.serena`, `cache`, `logs`, `reports`, `tmp` больше не шумят как untracked runtime-артефакты
- Блокеры: root-cause flaky OpenClaw gateway пока не добит до server-side fix, потому что после локальных правок основной хост `95.216.242.28` перестал отвечать по `ssh`/ICMP с текущей машины. Последнее подтверждённое live-исправление для gateway — фиксация `tools.web.search.provider`; дальше нужно вернуться к permission/watcher handshake диагностике после восстановления доступа
- Следующий шаг: как только вернётся доступ к live-хосту, продолжить task по OpenClaw gateway, затем прогнать `verify_live_integrations.sh` и live `cloudbot_runtime_verify`, после чего можно считать cleanup цикла завершённым

- Дата и время: `2026-03-29 11:50 MSK`
- Сценарий: `manual`
- Что сделано: завершена инвентаризация контуров, которые раньше не были явно учтены в общей карте системы. Подтверждено, что канонический runtime-контур кода остаётся только один: `/Users/pro2kuror/Desktop/OpenClo/projects/engineer`. Дополнительно классифицированы смежные локальные папки: `/Users/pro2kuror/Desktop/architect` как control-plane workspace, `/Users/pro2kuror/Desktop/OpenClo/projects/commercial-director` как внешний knowledge/migration contour без `.git`, `/Users/pro2kuror/Desktop/OpenClo/projects/whoop` как standalone WHOOP sandbox без `.git`, `/Users/pro2kuror/Desktop/OpenClo/archive/restored-workspace` как архивный git-контур в detached `HEAD`, `/Users/pro2kuror/Desktop/OpenClo/incubator/openclaw-extensions` как инкубационный non-git контур. На live-хосте отдельно подтверждены active auxiliary-контуры: generic sales runtime `/opt/cloudbot-runtime/current`, `cloudbot-sales-reports`, server-only `todo-integration`, WHOOP report layer и `cloudbot-bitrix-app.service` на `/opt/openclaw`. Эта классификация добавлена в `engineer/docs/architecture/system_map.md`
- Какие проверки запущены: `find /Users/pro2kuror/Desktop/OpenClo -maxdepth 3 -type d | sort`, проверка `.git`/branch/remote для `engineer`, `commercial-director`, `whoop`, `archive/restored-workspace`, `incubator/openclaw-extensions`, чтение `projects/commercial-director/assistant/README.md`, `projects/whoop/README.md`, `incubator/openclaw-extensions/README.md`, `archive/restored-workspace/AGENTS.md`, `rg` по `engineer/docs` и `architect/docs` на `source of truth`, `commercial-director`, `todo-integration`, `whoop`, `archive`, SSH-inspect `/etc/cron.d/{cloudbot-sales-reports,openclaw-whoop-report,openclaw-todo-digest}`, `systemctl cat cloudbot-bitrix-app.service`, листинг server wrapper’ов `/usr/local/bin/cloudbot-*` и `send_whoop_*`
- Результат проверок: новых production git-runtime repos кроме `engineer` не найдено; `commercial-director` и `whoop` существуют как локальные папки без git-контракта и не должны автоматически считаться production source of truth; `restored-workspace` и `openclaw-extensions` подтверждены как archival/incubator, а не активный runtime. На сервере обнаружены и формально зафиксированы дополнительные active operational-контуры: sales, WHOOP, Bitrix app и legacy `todo-integration`; именно они создавали неполную картину системы, если смотреть только на `larisa` и `engineer`
- Блокеры: классификация выполнена, но сами auxiliary/server-only контуры всё ещё частично живут вне git-managed deploy-модели; особенно это касается `todo-integration`, WHOOP-скриптов и `cloudbot-bitrix-app.service`
- Следующий шаг: перейти к устранению flaky OpenClaw gateway path, затем к weather/Open-Meteo деградации, после чего отдельно решать, какие из auxiliary-контуров нужно переносить под git/deploy-контроль, а какие формально оставлять отдельными доменами

- Дата и время: `2026-03-29 11:46 MSK`
- Сценарий: `manual`
- Что сделано: закрыты два первых пункта стабилизации runtime-контура. В инженерном репозитории введён единый контракт расписаний `configs/schedule_contract.env`, на него переведены `openclaw_healthcheck_schedule`, `larisa_agent_deploy`, `sales_agent_deploy`, `todo_digest_schedule` и локальный `configs/schedules.cron`; устаревший `checks/instruction_conflicts.sh` подтянут к этому контракту. На live-хосте `ams-1-vm-76ds` подтверждено и сохранено состояние: `healthcheck-daily` работает в `09:00 MSK`, `daily-status-report` в `09:30 MSK`, legacy `digest:morning` и `digest:midday` в `/etc/cron.d/openclaw-todo-digest` остаются закомментированными. Отдельно снята двусмысленность runtime-root для Ларисы: подтверждено, что `/opt/cloudbot-runtime/larisa/current` является каноническим runtime Ларисы, а общий `/opt/cloudbot-runtime/current` сейчас используется отдельными sales-wrapper’ами; поэтому `cloudbot_runtime_rollback.sh`, `cloudbot_runtime_unlock.sh` и `cloudbot_runtime_verify.sh` переведены на scoped runtime Ларисы по умолчанию, а архитектурные документы обновлены под эту модель
- Какие проверки запущены: `bash -n infra/orchestrator/lib.sh infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh infra/orchestrator/workflows/larisa_agent_deploy.sh infra/orchestrator/workflows/sales_agent_deploy.sh infra/orchestrator/workflows/todo_digest_schedule.sh`, `make openclaw.healthcheck-schedule MODE=inspect DRY_RUN=1`, `make openclaw.todo-digest-schedule MODE=inspect DRY_RUN=1`, `bash checks/instruction_conflicts.sh`, `bash checks/post_change_verify.sh`, `python3 checks/system_test.py`, `DRY_RUN=0 make openclaw.healthcheck-schedule MODE=inspect`, `DRY_RUN=0 make openclaw.todo-digest-schedule MODE=inspect`, `bash -n infra/orchestrator/workflows/cloudbot_runtime_{rollback,unlock,verify}.sh`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_unlock inspect`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_rollback inspect`, `./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, SSH-inspect server wrapper’ов `/usr/local/bin/cloudbot-larisa-daily-brief.sh` и `/usr/local/bin/cloudbot-sales-daily-brief.sh`
- Результат проверок: все локальные синтаксические и системные тесты зелёные (`SYSTEM TEST OK`, `post_change_verify: Итог: ОК`); live inspect подтверждает `healthcheck-daily`=`0 9 * * * Europe/Moscow`, `daily-status-report`=`30 9 * * * Europe/Moscow`, обе job включены; `todo_digest_schedule inspect` показывает `before.morning=commented`, `before.midday=commented`; `cloudbot_runtime_verify` вернул `Итог: ОК`, явно показал `current_link=/opt/cloudbot-runtime/larisa/current`, `generic_current_link=/opt/cloudbot-runtime/current` и подтвердил, что larisa-wrapper использует scoped current link. Дополнительно зафиксировано, что первый вызов `openclaw cron list` всё ещё иногда падает через `gateway closed (1000)`, но текущий retry это уже переживает
- Блокеры: не устранена первопричина flaky OpenClaw gateway path; не разобран weather/Open-Meteo `SSL EOF`; не завершена полная карта контуров за пределами `engineer` и live runtime (`commercial-director`, `whoop`, `archive/restored-workspace`, `incubator/openclaw-extensions`, server-only `todo-integration`, sales/WHOOP auxiliary layers)
- Следующий шаг: собрать и классифицировать все оставшиеся контуры как active / auxiliary / archive, затем отдельно починить gateway flakiness и weather-деградацию, после чего перейти к git hygiene и разделению локальных verify от live/server verify

- Дата и время: `2026-03-26 12:42 MSK`
- Сценарий: `manual`
- Что сделано: выполнено точечное timezone-исправление активного Python-контура Ларисы Ивановны для `daily brief / day report`; проблема была в том, что календарные времена местами проходили в report как сырые строки источника и выводились/сортировались без гарантированной нормализации в `Europe/Moscow`; в инженерном контуре добавлен компактный helper `agents/larisa_ivanovna/timezone.py`, после чего календарный provider теперь нормализует входящие `DATE_FROM/DATE_TO` в IANA timezone `Europe/Moscow`, рендер `telegram_brief` и `telegram_meetings` больше не режет строку времени напрямую, а переводит его в Москву, а `build_day_brief_request` больше не зависит от локального timezone окружения при построении даты заголовка
- Какие проверки запущены: `python3 -m unittest tests.test_larisa_agent`, `python3 -m py_compile agents/larisa_ivanovna/timezone.py agents/larisa_ivanovna/providers/calendar_provider.py agents/larisa_ivanovna/workflows/daily_brief.py agents/larisa_ivanovna/formatters/telegram_brief.py agents/larisa_ivanovna/formatters/telegram_meetings.py cloudbot/workflows/larisa_runtime.py`, `python3 checks/smoke_test.py`
- Результат проверок: `17/17 OK`, `SMOKE TEST OK`; добавлены тесты на Moscow-заголовок дня, Moscow-конвертацию UTC-события в Bitrix provider и Moscow-рендер времени встречи внутри самого brief; timezone теперь считается строго через `Europe/Moscow`, без hardcoded `+3`
- Блокеры: live боевой слот после этой timezone-правки ещё не наблюдался; серверный legacy secretary-render со скриншота уже не является активным morning source после предыдущего cutover, но его snapshot по-прежнему лежит в server-only архиве
- Следующий шаг: при ближайшем живом `day report` подтвердить, что дата заголовка, встречи и фокус-блоки отображаются по Москве; затем продолжить зачистку оставшегося legacy `evening/reminders/execution`

- Дата и время: `2026-03-26 12:34 MSK`
- Сценарий: `manual`
- Что сделано: после live cutover `digest:midday` добрана точная инвентаризация минутных legacy-job в `openclaw-todo-digest`; подтверждено, что `execution:tick` имеет живой state и историю post-meeting советов до `2026-03-26`, `reminders:tick` остаётся условно активным при timed-задачах, а `focus:tick` стал orphaned слоем после снятия legacy `morning`: его producer `saveFocusProposalForDate(...)` жил только в legacy `send-digest.mjs` на слоте `morning`, при этом в live state-root `/home/node/.openclaw/todo-integration-data` отсутствует `focus_blocks.json`, а `focus_proposal.json` содержит `proposal: null`; на хосте `ams-1-vm-76ds` отключена строка `npm run focus:tick` в `/etc/cron.d/openclaw-todo-digest` с backup в `/var/backups/openclaw-focus-disable-20260326_093302_MSK`
- Какие проверки запущены: SSH-inspect логов `/var/log/openclaw-todo-reminders.log`, `/var/log/openclaw-focus-tick.log`, `/var/log/openclaw-execution-tick.log`, просмотр live `.env.runtime` и `TODO_STATE_DIR`, чтение state-файлов `/home/node/.openclaw/todo-integration-data/{execution_settings.json,execution_state.json,reminders_state.json,focus_proposal.json}`, контроль `/etc/cron.d/openclaw-todo-digest` после отключения `focus:tick`
- Результат проверок: `execution_settings.json` содержит `enabled=true`, а `execution_state.json` хранит живую историю post-meeting событий вплоть до сегодняшнего дня; `reminders_state.json` существует и обновляется, хотя сейчас `entries=[]` и live-log показывает `checked=0`; `focus` не имеет живого producer/state и не несёт активной функции, поэтому его cron безопасно снят; в active legacy cron остались только `digest:evening`, `reminders:tick`, `execution:tick` и sync
- Блокеры: `evening` ещё не переведён в Ларису, а `reminders/execution` остаются server-only доменами вне git-managed runtime; отключать их blind нельзя, потому что `execution` по факту шлёт живые post-meeting и next-action советы, а `reminders` зависит от timed-задач и может снова ожить без изменения кода
- Следующий шаг: решить судьбу `digest:evening` после сравнения с live-поведением legacy; затем отдельно разобрать `execution:tick` как самостоятельный домен meeting-driven советов и решить, переносить ли его в Ларису или формально выделять из её контура; `reminders:tick` оставить последним шагом после проверки, нужны ли timed-reminders как часть Ларисы

- Дата и время: `2026-03-26 12:31 MSK`
- Сценарий: `manual`
- Что сделано: подтверждён штатный утренний слот Ларисы в `09:00 MSK` без legacy-дубля; после этого продолжен безопасный поэтапный cutover `todo-integration`: в инженерном контуре уже подготовленные `midday/evening` workflow Ларисы (`80ff599`, `Починить deploy midday evening runners`) использованы для live-перевода только сценария `digest:midday`; на хосте `ams-1-vm-76ds` повторно выполнен `larisa_agent_deploy` с `LARISA_ENABLE_MIDDAY_CRON=1`, в `/etc/cron.d/cloudbot-larisa-daily-brief` добавлен слот `0 14 * * *` для `/usr/local/bin/cloudbot-larisa-midday-replan.sh`, а строка legacy `npm run digest:midday` в `/etc/cron.d/openclaw-todo-digest` отключена с backup в `/var/backups/openclaw-todo-midday-disable-20260326_092816_MSK`; `digest:evening`, `reminders:tick`, `focus:tick`, `execution:tick` оставлены без изменений
- Какие проверки запущены: SSH-inspect live dry-run и cron через `./ops/ssh_happ.sh primary`, сравнение `/opt/cloudbot-runtime/larisa/current/reports/larisa_midday_replan_*.txt` и tail `/var/log/openclaw-todo-midday.log`, `LARISA_ENABLE_MIDDAY_CRON=1 ALLOW_DIRTY_DEPLOY=1 ./infra/orchestrator/run_workflow.sh larisa_agent_deploy`, контроль `/etc/cron.d/cloudbot-larisa-daily-brief`, `/etc/cron.d/openclaw-todo-digest`, ручной `TELEGRAM_DRY_RUN=1 LARISA_TELEGRAM_DRY_RUN=1 /usr/local/bin/cloudbot-larisa-midday-replan.sh`, `python3 -m unittest tests.test_larisa_agent tests.test_bitrix_sales_adapter tests.test_lev_petrovich_runtime`, `python3 checks/smoke_test.py`
- Результат проверок: утренний боевой send Ларисы подтверждён пользователем как единственный и корректный; live `midday` слой Ларисы успешно задеплоен в dedicated runtime `/opt/cloudbot-runtime/larisa/releases/codex_feature_self-healing_80ff599`; legacy `midday` больше не активен в cron; dry-run `cloudbot-larisa-midday-replan.sh` завершился успешно и сохранил свежий отчёт `larisa_midday_replan_20260326_122816_MSK.txt`; `40/40` unit-tests зелёные, `SMOKE TEST OK`; `evening` dry-run Ларисы уже рендерится корректно, но боевой слот `19:00 MSK` пока остаётся за legacy-контуром
- Блокеры: персональный assistant-контур всё ещё не сведен в один слой, потому что живы `digest:evening`, `reminders:tick`, `focus:tick`, `execution:tick` в server-only `todo-integration`; `evening` уже имеет repo-managed кандидат в Ларисе, но ещё не подтвержден пользовательским сравнением против legacy-поведения
- Следующий шаг: отдельно верифицировать `evening` на реальных задачах и решить, переводить ли `19:00 MSK` в Ларису; затем разобрать минутные legacy-сценарии `reminders:tick`, `focus:tick`, `execution:tick` на перенос в repo-managed контур или на осознанное выделение в отдельный домен

- Дата и время: `2026-03-26 09:49 MSK`
- Сценарий: `manual`
- Что сделано: завершён live cutover утреннего overlap между Ларисой и legacy `openclaw-todo-digest`; в инженерном контуре усилен `agents/larisa_ivanovna`: daily brief теперь строит фокус-блоки на основе задач и свободных окон, а deploy раннер Ларисы умеет читать отдельный server-only env `/etc/openclaw/larisa.env`; изменения закоммичены в `codex/feature/self-healing` (`75132f6`, `Усилить утренний контур Ларисы`) и запушены в GitHub; на live-хосте `ams-1-vm-76ds` выкатился новый release `/opt/cloudbot-runtime/larisa/releases/codex_feature_self-healing_75132f6`, создан `/etc/openclaw/larisa.env` с `LARISA_TODO_TOKEN`, Larisa cron переведён на ежедневный `09:00 MSK`, а строка `digest:morning` в `/etc/cron.d/openclaw-todo-digest` отключена с backup в `/var/backups/openclaw-todo-morning-disable-20260326_064835_MSK`
- Какие проверки запущены: `python3 -m unittest tests.test_larisa_agent tests.test_bitrix_sales_adapter tests.test_lev_petrovich_runtime`, `python3 checks/smoke_test.py`, `python3 -m compileall -q agents/larisa_ivanovna`, `bash -n infra/orchestrator/workflows/larisa_agent_deploy.sh`, ручной локальный сценарный прогон `get_day_brief`, `ALLOW_DIRTY_DEPLOY=1 ./infra/orchestrator/run_workflow.sh larisa_agent_deploy`, `ALLOW_DIRTY_DEPLOY=1 ./infra/orchestrator/run_workflow.sh cloudbot_runtime_verify`, live dry-run `/usr/local/bin/cloudbot-larisa-daily-brief.sh`, контроль `/etc/cron.d/cloudbot-larisa-daily-brief`, `/etc/cron.d/openclaw-todo-digest`, `/etc/openclaw/larisa.env`
- Результат проверок: тесты зелёные (`37 OK` + `SMOKE TEST OK`); `cloudbot_runtime_verify` вернул `Итог: ОК`; свежий dry-run отчёт Ларисы `larisa_daily_brief_20260326_094808_MSK.txt` содержит живые задачи Todoist, фокус-блоки `10:00-11:00` и `12:00-13:00`, погоду и Telegram route `larisa-ivanovna`; `cloudbot-larisa-daily-brief` теперь daily `09:00 MSK`; legacy `digest:morning` больше не активен в cron, при этом `digest:midday`, `digest:evening`, `reminders:tick`, `focus:tick`, `execution:tick` оставлены включёнными
- Блокеры: после cutover ещё не наблюдался следующий штатный боевой запуск Ларисы в `09:00 MSK`, поэтому факт реальной доставки после снятия legacy `morning` пока подтверждён dry-run, а не очередным scheduled send; в legacy-контуре `todo-integration` остаются живые сценарии `midday/evening/reminders/focus/execution`, которые пока не перенесены в Ларису и продолжают быть отдельным server-only источником поведения
- Следующий шаг: после ближайшего реального слота `09:00 MSK` проверить, что пришёл ровно один утренний отчёт Ларисы и не было дубля из legacy; затем отдельно разбирать `digest:midday`, `digest:evening`, `reminders:tick`, `focus:tick`, `execution:tick` на перенос или выключение по одному сценарию

- Дата и время: `2026-03-25 09:45 MSK`
- Сценарий: `manual`
- Что сделано: найден и точечно отключён live-источник утренней карточки `🧭 Сценарий дня`; подтверждено, что exact report приходит не из `cloudbot-larisa-daily-brief`, а как extra-message внутри legacy/server-only `openclaw-todo-digest` на хосте `ams-1-vm-76ds`: cron `30 6 * * *` UTC (`09:30 MSK`) запускает `docker exec openclaw-openclaw-gateway-1 ... npm run digest:morning`, а active renderer живёт в `/root/.openclaw/workspace/todo-integration/src/execution/timelineBuilder.mjs` и вызывается из `/root/.openclaw/workspace/todo-integration/src/send-digest.mjs`; для безопасного отключения добавлен отдельный флаг `DAY_SCENARIO_MESSAGE_ENABLED`, отправка карточки gated в `src/send-digest.mjs`, а в live `.env.runtime` установлен `DAY_SCENARIO_MESSAGE_ENABLED=0`
- Какие проверки запущены: SSH-inspect через `./ops/ssh_happ.sh primary`, просмотр `/etc/cron.d/openclaw-todo-digest`, grep по live container `todo-integration`, проверка mount `/root/.openclaw -> /home/node/.openclaw`, `node --check src/send-digest.mjs`, `node --check src/config.mjs`, `node src/selftest.mjs`, ручной dry-run утреннего digest с mocked Telegram transport и временным `TODO_STATE_DIR`, финальный grep по live `src` и `.env.runtime`, контрольный grep по Larisa runtime/path
- Результат проверок: exact source подтверждён только в live `todo-integration`; `larisa_daily_brief` не является источником этого сообщения; после правки `selftest: ok`, синтаксис файлов корректен, dry-run показал `SCENARIO_ABSENT`, `sent slot=morning ... extras=0`; cron `openclaw-todo-digest`, reminders, sync, midday/evening digest и `execution:tick` оставлены без изменений; входящего reply-handler для `Сценарий дня` не найдено, сценарий outbound-only
- Блокеры: source of truth этого legacy/server-only контура отсутствует в локальных git-репозиториях `architect`/`engineer`, поэтому изменение выполнено напрямую в host-mounted runtime с бэкапом `/var/backups/todo-day-scenario-disable_20260325_093550_MSK`
- Следующий шаг: при ближайшей maintenance-сессии вынести `todo-integration` server-only код под git-контроль или оформить отдельный orchestrator workflow, чтобы такие runtime-only флаги и сценарии не оставались вне репозитория

- Дата и время: `2026-03-25 08:52 MSK`
- Сценарий: `manual`
- Что сделано: на live-хосте `ams-1-vm-76ds` подтвержден и отключён server-only сценарий вечернего WHOOP/Telegram check-in; найден активный trigger `/etc/cron.d/openclaw-whoop-checkin`, который в `20:30 MSK` запускал `/usr/local/bin/send_whoop_checkin_prompt.py` с точным текстом `Вечерний чек-ин (0-10)`; активный cron удалён, исходный cron и script сохранены в `/var/backups/openclaw-whoop-checkin-20260325_085125_MSK`, а `/usr/local/bin/send_whoop_checkin_prompt.py` заменён на безопасный no-op stub, который больше не отправляет сообщение
- Какие проверки запущены: SSH-inspect через `./ops/ssh_happ.sh primary`, просмотр `/etc/cron.d/openclaw-whoop-checkin`, `/etc/cron.d/openclaw-whoop-report`, `/etc/cron.d/openclaw-todo-digest`, просмотр live `/usr/local/bin/send_whoop_checkin_prompt.py`, grep по `/usr/local/bin`, `/opt/openclaw`, `/home/node/.openclaw/workspace/todo-integration`, tail `/var/log/openclaw-whoop-checkin.log`, ручной запуск stub `sudo /usr/local/bin/send_whoop_checkin_prompt.py`, повторная проверка отсутствия exact prompt и отсутствия `checkin`-parser refs в live `/usr/local/bin/send_whoop_report.py`
- Результат проверок: активный источник сценария был единственный и server-only; exact prompt больше не присутствует в активных cron/script файлах; manual run stub подтверждает no-op `WHOOP evening check-in disabled; no message sent.`; live `send_whoop_report.py` не содержит активного обработчика `checkin`; cron `openclaw-whoop-report`, `cloudbot-larisa-daily-brief` и `openclaw-todo-digest` остались на месте без изменений; дублей сценария в `projects/engineer`, `commercial-director/assistant`, `todo-integration` и активном `/opt/openclaw` не найдено
- Блокеры: server-only WHOOP check-in не имеет зафиксированного source of truth в git-контурах `architect`/`engineer`, поэтому изменение выполнено напрямую на live-хосте с бэкапом и не отражено в кодовой базе
- Следующий шаг: при первой удобной maintenance-сессии формализовать управление server-only WHOOP cron/script через репозиторий или отдельный orchestrator workflow, чтобы подобные runtime-only сценарии больше не жили вне git-контроля

- Дата и время: `2026-03-25 08:04 MSK`
- Сценарий: `manual`
- Что сделано: подтверждена live-диагностика OpenClaw на хосте `ams-1-vm-76ds`; установлено, что падение `daily-status-report` в `2026-03-25 07:50:00 MSK` было транзиентным и уже самовосстановилось успешным повторным run в `2026-03-25 07:50:50 MSK`; в инженерном контуре обновлён workflow `infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`, чтобы `healthcheck-daily` и `daily-status-report` трактовали `missing scope: operator.read`, `multi_user_heuristic`, единичный self-healed cron error и доступное обновление OpenClaw как деградированную диагностику/техдолг, а не как безусловную активную поломку; эти payload-правки применены на live-хосте без изменения боевых расписаний `07:30` и `07:50` МСК; в `architect` синхронизированы `docs/prompts/daily-health-check.md` и `docs/checklists/health-check.md`
- Какие проверки запущены: `openclaw status --deep` на хосте через SSH-контур, `openclaw cron list --all --json`, `openclaw cron runs --id 5eebd12d-ee0b-4e9d-aea5-a55542f9f336 --limit 10`, `openclaw devices list`, `openclaw doctor --non-interactive`, `bash -n /Users/pro2kuror/Desktop/OpenClo/projects/engineer/infra/orchestrator/workflows/openclaw_healthcheck_schedule.sh`, `cd /Users/pro2kuror/Desktop/OpenClo/projects/engineer && bash checks/instruction_conflicts.sh`, `HEALTH_CRON_EXPR='30 7 * * *' STATUS_CRON_EXPR='50 7 * * *' ... make openclaw.healthcheck-schedule MODE=apply`, повторный `make openclaw.healthcheck-schedule MODE=inspect`, просмотр свежего `/tmp/openclaw/openclaw-2026-03-25.log`
- Результат проверок: live job payload’ы обновлены и подтверждены повторным inspect; `healthcheck-daily` и `daily-status-report` остались включены с расписанием `07:30` и `07:50` МСК; текущие `lastRunStatus` у обеих job — `ok`; синтаксис workflow корректен, `instruction_conflicts.sh` проходит; в логах нет ошибок применения cron edit; падение LLM-провайдера не является текущим инцидентом, потому что следующий run уже завершился успешно
- Блокеры: `operator.read` для полного deep probe в CLI-path по-прежнему не закрыт отдельным операторским credential path и остаётся деградированной диагностикой, а не отказом gateway; warning `multi_user_heuristic` остаётся осознанным архитектурным риском shared-access модели и не может быть снят без более глубокого изменения trust boundary; обновление OpenClaw до `2026.3.23-2` по-прежнему отложено до окна обслуживания
- Следующий шаг: дождаться следующего штатного прогона `healthcheck-daily` в `2026-03-26 07:30 MSK` и `daily-status-report` в `2026-03-26 07:50 MSK`, чтобы подтвердить новую классификацию в живом отчёте; отдельно подготовить окно обслуживания для обновления OpenClaw и, при необходимости, отдельный операторский credential path для полного deep probe

- Дата и время: `2026-03-22 17:51 MSK`
- Сценарий: `manual`
- Что сделано: добавлены структура модулей, шаблон `scripts/deploy.sh`, документы для nightly-run и health-check
- Какие проверки запущены: `bash -n scripts/deploy.sh`, визуальная проверка структуры файлов
- Результат проверок: синтаксис `deploy.sh` корректен, каркас файлов создан
- Блокеры: отсутствует исходный код проекта, git-репозиторий, тесты, список сервисов и рабочие логи
- Следующий шаг: перенести каркас в реальный репозиторий Cloudbot, описать сервисы и команды запуска, затем подключить Codex automation

## Текущий статус

- Дата и время: `2026-03-22 18:50 MSK`
- Сценарий: `manual`
- Что сделано: в `AGENTS.md` и `docs/workflows/codex-github.md` зафиксирован постоянный контракт по git-топологии проекта: `architect` как documentation/control-plane workspace, `/Users/pro2kuror/Desktop/OpenClo/projects/engineer` как канонический инженерный git-репозиторий с `origin=https://github.com/Johnnie13Walker/codex-base.git`, `/opt/openclaw` как отдельный server-only repo; отдельно закреплено правило больше не спрашивать пользователя про существование git и расположение основного repo, если путь `projects/engineer` доступен
- Какие проверки запущены: `date '+%Y-%m-%d %H:%M:%S %Z'`, `git -C /Users/pro2kuror/Desktop/OpenClo/projects/engineer rev-parse --abbrev-ref HEAD`, `git -C /Users/pro2kuror/Desktop/OpenClo/projects/engineer remote get-url origin`, визуальная сверка `AGENTS.md` и `docs/workflows/codex-github.md`
- Результат проверок: подтверждены текущая ветка `codex/feature/self-healing` и `origin` инженерного контура; инструкции обновлены и теперь явно задают, где искать код, где искать документацию и в каких случаях вопрос про git вообще допустим
- Блокеры: server-only контур `/opt/openclaw` и его git-состояние по-прежнему не видны из этой рабочей копии; часть live runtime остается вне локально проверяемого контура
- Следующий шаг: опираться на `projects/engineer` как на дефолтный source repo для всех кодовых задач и больше не эскалировать вопрос про git без реального расхождения в локальной конфигурации

## Актуальный статус

- Дата и время: `2026-03-22 18:33 MSK`
- Сценарий: `manual`
- Что сделано: каталог инициализирован как локальный git-репозиторий, текущие файлы проекта добавлены в git index
- Какие проверки запущены: `git rev-parse --is-inside-work-tree`, `git init -b main`, `git add`, `git status --short --branch`, `git rev-parse --show-toplevel --is-bare-repository`, `git diff --cached --stat`
- Результат проверок: git-репозиторий создан, рабочее дерево не bare, файлы staged для первого коммита
- Блокеры: remote не настроен, коммит ещё не создан, автоматические тесты в репозитории не обнаружены, прикладные логи отсутствуют
- Следующий шаг: при необходимости сделать первый commit, настроить `origin`, затем создать `dev` и рабочие feature-ветки

## Последнее изменение

- Дата и время: `2026-03-22 18:33 MSK`
- Сценарий: `manual`
- Что сделано: prompt для работы в feature-ветке обновлён, теперь ветка и базовая ветка должны сначала подтверждаться инженером
- Какие проверки запущены: визуальная проверка `docs/prompts/feature-branch-task.md`
- Результат проверок: логика prompt обновлена
- Блокеры: имя рабочей ветки и базовой ветки пока не подтверждены инженером
- Следующий шаг: получить от инженера название целевой ветки и базовой ветки, затем продолжить git workflow
